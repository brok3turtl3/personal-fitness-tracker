---
phase: 02-diet-ux-overhaul
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-03"]
files_modified:
  - src/app/features/diet/diet-page.component.ts
  - src/app/features/diet/diet-page.component.spec.ts
autonomous: true
requirements: [DIET-01, DIET-04, DIET-05, DIET-06, DIET-09]

must_haves:
  truths:
    - "Searching a non-existent food surfaces an inline 'Add as new food' affordance that saves to the library AND stages it in the current meal — without leaving the flow (DIET-01)"
    - "The food picker offers match-as-you-type, Recent (on focus), and an auto-ranked Frequent group (DIET-04)"
    - "'Repeat yesterday' / 'Copy from another day' lands prior items as editable pending items (DIET-05)"
    - "Daily totals (kcal/protein/fat/carbs/net carbs) update live as pending items change; %-of-target bars render when targets are set, over-target uses destructive color + a text label (DIET-06)"
    - "All destructive confirms are inline button-swaps — no window.confirm, no modal"
    - "The unit picker only offers cross-dimension units when the food has densityGramsPerMl (DIET-03 graceful fallback)"
    - "D-01: the quick-add affordance is inline within the meal-log flow (reuses .inline-form inside the meal panel) — never a modal and never a context switch to the Saved Foods panel"
    - "D-02: macro entry is frictionless MANUAL entry — the inline form takes typed macros directly, with NO external food-DB lookup or autocomplete"
    - "D-06: the food picker is case-insensitive substring match, shows Recent on focus, and an auto-ranked 'Frequent' group — never manual starring/favoriting"
    - "D-14: no external food DB and no new network egress is introduced (Phase 5 CSP lock upheld — T-EGRESS)"
  artifacts:
    - path: "src/app/features/diet/diet-page.component.ts"
      provides: "Overhauled meal-log UX: inline quick-add, search/recents/frequent, copy-meal, live totals + target bars, inline confirms"
      contains: "filterFoods"
  key_links:
    - from: "diet-page.component.ts onAddMealItem"
      to: "src/app/services/units.ts"
      via: "toBaseUnits delegation (toBaseUnitsForPreview deleted)"
      pattern: "from '.*/services/units'"
    - from: "diet-page.component.ts food picker"
      to: "src/app/services/food-ranking.ts"
      via: "filterFoods + rankFoods + recentFoods"
      pattern: "food-ranking"
---

<objective>
Overhaul `diet-page.component.ts` (UI-only — business logic stays in DietService/units.ts/food-ranking.ts) to deliver the headline friction-killers: inline quick-add when search misses, match-as-you-type with Recent + auto-ranked Frequent groups, one-action copy-a-meal, live daily totals with optional %-of-target bars, and inline button-swap confirms replacing every `window.confirm`. Delete the duplicate `toBaseUnitsForPreview` and delegate to `units.ts`.

Purpose: DIET-01 (inline quick-add), DIET-04 (search/recents/favorites), DIET-05 (copy-meal), DIET-06 (live totals + targets), DIET-09 (history renders snapshots) — the user-observable payoff of the phase.
Output: overhauled `diet-page.component.ts` + extended characterization spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-diet-ux-overhaul/02-UI-SPEC.md
@.planning/phases/02-diet-ux-overhaul/02-RESEARCH.md
@.planning/phases/02-diet-ux-overhaul/02-PATTERNS.md
@src/app/services/units.ts
@src/app/services/food-ranking.ts

<interfaces>
<!-- Verified live line numbers in diet-page.component.ts -->
- Templates/forms: addFoodForm (523, template form at 45), mealForm (239 template), mealItemForm (575, template at 264, inline-form).
- `pendingItems: Array<{ savedFoodId; servingId; quantity; label; preview: NutritionTotals }>` (545).
- `onAddMealItem()` (772) — stages a pending item; currently calls `toBaseUnitsForPreview(food, serving.unit, usedUnits)` (786). Pending push at 794-803.
- `removePendingItem(index)` (808). `onAddMeal()` (812-863 add/update branch + takeUntilDestroyed). `startEditMeal(meal)` (item→pending map at 869-875).
- `onDeleteFood(food)` (654, uses `window.confirm` at 655). `onDeleteMeal(meal)` (uses window.confirm). History meal render uses meal.totals + snapshot-derived item fields.
- `toBaseUnitsForPreview(food, unit, amount)` (988-1004) — DELETE; delegate to units.ts.
- Helpers: `formatLocalDateTime`, `formatLocalDateTimeFromIso`. `<app-empty-state>`/`<app-error-state>` already imported. `private destroyRef = inject(DestroyRef)` field present.

DietService (Plan 03): copyMealItems(meal, currentFoods), getMealsInRange, getMealsForDay, getDailyTargets/setDailyTargets/clearDailyTargets, addSavedFood, addMeal/updateMeal/deleteMeal, getSavedFoods, scaleFoodTotals/sumTotals (exported).
food-ranking.ts (Plan 01): filterFoods(foods, query), rankFoods(foods, mealEntries, now), recentFoods(foods, mealEntries, n).
units.ts (Plan 01): toBaseUnits, convertMeasured, isMeasuredUnit, UnitConversionError, MeasuredUnit.
</interfaces>

<ui-contract>
<!-- LOCKED copy + styling from 02-UI-SPEC.md — reuse global classes, no new tokens, no modal -->
- Inline quick-add trigger: "No match. Add \"{query}\" as a new food"; confirm CTA: "Add food & log it"; helper: "Enter macros once — this food stays in your library for next time."
- Density-absent fallback note: "No density set — you can log this food in weight or volume units, but not convert between them."
- Search placeholder "Search foods…"; group labels "Recent" and "Frequent" (NOT "Favorites").
- Copy triggers: "Repeat yesterday" (primary) + "Copy from another day…" (date-source picker); result: "Copied {n} items — edit or remove any before saving."
- Targets: "Set daily targets" / "Edit daily targets"; over-cap label "{value} / {target} {unit} · over by {n}".
- Target bar: track #ecf0f1, 8px tall, 999px radius; fill #2471a3 at/under, #c0392b over-target; label "{value} / {target} {unit} · {pct}%" ALWAYS present (color never the only signal).
- Reuse classes: .panel/.subpanel/.form-group/.form-row/.inline-form/.btn*/.pill/.pill-row/.totals/.totals-grid/.k/.v/.history-list/.history-item; <app-empty-state>/<app-error-state>.
- Inline confirms: Delete saved food → button swaps to "Delete \"{name}\"? This won't change past meals — [Confirm] [Cancel]"; Delete meal → "Delete this meal? [Confirm] [Cancel]"; Clear targets → "Clear daily targets? Totals will stop showing % of target. [Confirm] [Cancel]". Remove pending item → NO confirm (.btn-sm.btn-danger "Remove").
</ui-contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline quick-add + search/Recent/Frequent picker + delegate conversion + replace window.confirm</name>
  <files>src/app/features/diet/diet-page.component.ts, src/app/features/diet/diet-page.component.spec.ts</files>
  <read_first>
    - .planning/phases/02-diet-ux-overhaul/02-UI-SPEC.md (full — copy + reuse map + destructive-confirm table)
    - .planning/phases/02-diet-ux-overhaul/02-RESEARCH.md §Pattern 1 (inline quick-add, 175-188), §Pattern 3 (search/favorites, 199-202), §Pitfall 5 (density-gated unit picker, 297-301)
    - .planning/phases/02-diet-ux-overhaul/02-PATTERNS.md "diet-page.component.ts" section (lines 181-212)
    - src/app/features/diet/diet-page.component.ts (full — forms, onAddMealItem, onDeleteFood/onDeleteMeal, toBaseUnitsForPreview)
    - src/app/services/food-ranking.ts + src/app/services/units.ts (Plan 01 — the delegation targets)
  </read_first>
  <action>
Overhaul the food picker and quick-add in `diet-page.component.ts` (UI only; delegate all logic):
- SEARCH/RECENT/FREQUENT: add a search text control. On input change, render `filterFoods(savedFoods, query)` as the result list (`.history-list`/`.history-item` shape, placeholder "Search foods…"). On focus with an empty query, show two `.subpanel` groups: "Recent" = `recentFoods(savedFoods, meals, 8)` and "Frequent" = `rankFoods(savedFoods, meals, Date.now())` (top N) — render as `.pill`/`.pill-row`. Clicking a pill/result selects the food into `mealItemForm`. (Group labels EXACTLY "Recent"/"Frequent" per UI-SPEC; auto-ranked, no manual starring — D-06.)
- INLINE QUICK-ADD (D-01): when `filterFoods(...)` returns empty for a non-empty query, render the inline affordance "No match. Add \"{query}\" as a new food"; clicking expands the inline macro form (reuse `.inline-form` + `.form-row` + `.form-group` inside the meal panel — NOT the Saved Foods panel, NO modal) pre-filled with the typed name + helper "Enter macros once — this food stays in your library for next time." Macros are entered MANUALLY by typing — NO external food-DB lookup / autocomplete (D-02). Confirm button "Add food & log it" calls `dietService.addSavedFood(...)` then, on success, immediately stages the new food as a `pendingItem` (reuse the staging shape at lines 794-803). Pipe through `takeUntilDestroyed(this.destroyRef)`; map errors to `<app-error-state>`/field errors.
- UNIT PICker GATING (DIET-03): the unit `<select>` for a selected food only offers cross-dimension units when `food.densityGramsPerMl` is set; otherwise offer only the food's base-dimension measured units + its named servings, and show the fallback note "No density set — you can log this food in weight or volume units, but not convert between them." Never offer a unit that would force a density-less cross-dimension conversion (Pitfall 5).
- DELEGATE CONVERSION: in `onAddMealItem` (line 772), replace the `toBaseUnitsForPreview(...)` call (786) with the pure `toBaseUnits` from `units.ts`; DELETE `toBaseUnitsForPreview` (988-1004) entirely. Wrap in try/catch → on `UnitConversionError` show a field error rather than crashing.
- REPLACE window.confirm: `onDeleteFood` (654-655) and `onDeleteMeal` → inline button-swap confirms per the UI-SPEC table (a per-row `confirmingDeleteId` state; the Delete button swaps to the LOCKED confirm copy with [Confirm]/[Cancel] `.btn-sm`). REMOVE every `window.confirm(` call. Remove-pending-item stays no-confirm.
- Reuse `<app-empty-state>` with the LOCKED empty copy ("No saved foods yet" / "Nothing logged today" bodies from UI-SPEC).
  </action>
  <verify>
    <automated>ng test --no-watch --browsers=ChromeHeadless --include='**/diet-page.component.spec.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'toBaseUnitsForPreview' src/app/features/diet/diet-page.component.ts` returns 0 (deleted)
    - `grep -c 'window.confirm' src/app/features/diet/diet-page.component.ts` returns 0 (replaced)
    - `grep -c 'filterFoods\|rankFoods\|recentFoods' src/app/features/diet/diet-page.component.ts` returns at least 1
    - `grep -c "from '.*services/units'" src/app/features/diet/diet-page.component.ts` returns 1
    - `grep -c 'Frequent' src/app/features/diet/diet-page.component.ts` returns at least 1 AND `grep -c 'Favorites' ...` returns 0
    - `grep -c 'Add food & log it' src/app/features/diet/diet-page.component.ts` returns at least 1
    - `ng test --no-watch --browsers=ChromeHeadless --include='**/diet-page.component.spec.ts'` exits 0
  </acceptance_criteria>
  <done>Inline quick-add saves+logs without leaving the flow; search/Recent/Frequent picker wired to food-ranking; unit picker density-gated; toBaseUnitsForPreview deleted + delegated to units.ts; all window.confirm replaced by inline swaps; spec green.</done>
</task>

<task type="auto">
  <name>Task 2: Live daily totals + %-of-target bars + targets editor + copy-a-meal + extend characterization spec</name>
  <files>src/app/features/diet/diet-page.component.ts, src/app/features/diet/diet-page.component.spec.ts</files>
  <read_first>
    - .planning/phases/02-diet-ux-overhaul/02-UI-SPEC.md (target-bar styling + copy + targets-editor copy + copy-meal copy)
    - .planning/phases/02-diet-ux-overhaul/02-RESEARCH.md §Pattern 4 (live totals + bars, 204-211), §Pattern 6 (copy-meal, 226-229)
    - .planning/phases/02-diet-ux-overhaul/02-PATTERNS.md "diet-page.component.ts" live-totals + copy-meal lines (185-208)
    - src/app/features/diet/diet-page.component.ts (pendingItems, onAddMeal, startEditMeal map, .totals/.totals-grid markup)
    - src/app/features/diet/diet-page.component.spec.ts (existing characterization spec to extend) + src/app/services/diet.service.ts copyMealItems/getDailyTargets (Plan 03)
  </read_first>
  <action>
In `diet-page.component.ts`:
- LIVE TOTALS (DIET-06): compute live totals = `sumTotals([...savedTodayMeals.map(m => m.totals), ...pendingItems.map(p => p.preview)])` and recompute on every pending-item change (add/remove/quick-add). Render kcal/protein/fat/carbs/NET CARBS in the existing `.totals`/`.totals-grid`/`.k`/`.v` markup — net carbs always shown.
- TARGET BARS (D-08): load `dietService.getDailyTargets()`. When a metric target is set, render a `.target-bar` below its total: track `#ecf0f1`, 8px tall, 999px radius; fill `#2471a3` at/under target, `#c0392b` when over; ALWAYS render the text label "{value} / {target} {unit} · {pct}%" (or the over-cap copy "{value} / {target} {unit} · over by {n}") so color is never the sole signal. Compute `pct = round(value/target*100)`; clamp the bar WIDTH at 100% but show true % in the label.
- TARGETS EDITOR: an inline editor (reuse `.form-group`) opened via "Set daily targets"/"Edit daily targets"; Save → `dietService.setDailyTargets(...)` (validated in the service); Clear → inline confirm "Clear daily targets? Totals will stop showing % of target. [Confirm] [Cancel]" → `clearDailyTargets()`. takeUntilDestroyed on all subscriptions.
- COPY-A-MEAL (DIET-05): "Repeat yesterday" button → `getMealsForDay(yesterdayLocalKey)` then for each meal call `dietService.copyMealItems(meal, savedFoods)` and append to `pendingItems`; "Copy from another day…" → a `.form-group` `<input type="date">`/select source picker → `getMealsForDay(pickedKey)` → same map. Show the inline result "Copied {n} items — edit or remove any before saving." Items land EDITABLE in pendingItems (not committed). Derive the yesterday key via the shared local-time keying (reuse formatLocal/toDateKey — do NOT hand-roll UTC math).
Extend `src/app/features/diet/diet-page.component.spec.ts` (characterization-style DOM-shape assertions, inline axe-core via the existing helper, gated serious|critical):
- Live totals update when a pending item is added (assert the net-carbs cell reflects the sum).
- %-of-target bar renders with a text label when a target is set; over-target shows the destructive state + over-by label.
- "Repeat yesterday" populates pendingItems from a prior day's meal (editable).
- History list renders logged-meal snapshots (DIET-09 — values come from snapshot, not re-resolved).
  </action>
  <verify>
    <automated>ng test --no-watch --browsers=ChromeHeadless --include='**/diet-page.component.spec.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'sumTotals' src/app/features/diet/diet-page.component.ts` returns at least 1 (live totals)
    - `grep -c 'getDailyTargets\|setDailyTargets' src/app/features/diet/diet-page.component.ts` returns at least 1
    - `grep -c 'copyMealItems\|Repeat yesterday' src/app/features/diet/diet-page.component.ts` returns at least 1
    - `grep -c 'over by' src/app/features/diet/diet-page.component.ts` returns at least 1 (over-target text label — color not sole signal)
    - diet-page.component.spec asserts live-totals update on pending add AND target-bar label presence
    - `ng test --no-watch --browsers=ChromeHeadless --include='**/diet-page.component.spec.ts'` exits 0
  </acceptance_criteria>
  <done>Live totals incl. net carbs recompute on pending change; %-of-target bars with always-present text labels + over-target destructive state; targets editor with inline clear-confirm; copy-a-meal lands editable pending items; characterization spec extended; spec green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-entered food name / macros → DOM render | names render via Angular interpolation |
| user input → DietService | quick-add macros, targets, copy-meal flow re-validate in the service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering / Elevation | food-name XSS | mitigate | All user-entered names render via `{{ }}` interpolation (auto-escaped); NO `[innerHTML]`/`bypassSecurityTrust*` introduced — RESEARCH verified none exist; grep gate in verification (T-XSS) |
| T-02-04-02 | Tampering | density-less cross-dimension unit offered | mitigate | Unit picker gates cross-dimension units on `densityGramsPerMl`; onAddMealItem try/catches UnitConversionError → field error, never a silent wrong macro (Pitfall 5, DIET-03) |
| T-02-04-03 | Information disclosure | external food-DB egress | mitigate | Quick-add is MANUAL entry only (D-02/D-14); NO fetch to a nutrition API introduced; grep gate confirms no new network call (T-EGRESS, Phase 5 CSP) |
| T-02-04-04 | Tampering | snapshot immutability on render | mitigate | History list reads `item.snapshot`/`meal.totals`, never re-resolves the live food (Pitfall 4, D-12); copy-meal re-derive is for a NEW meal only |
</threat_model>

<verification>
- Full suite `ng test --no-watch --browsers=ChromeHeadless` green.
- `ng build --configuration=production` exits 0.
- `grep -rnE "innerHTML|bypassSecurityTrust|fetch\(|XMLHttpRequest" src/app/features/diet/diet-page.component.ts` returns no matches (T-XSS / T-EGRESS).
- `grep -c 'window.confirm' src/app/features/diet/diet-page.component.ts` returns 0.
</verification>

<success_criteria>
- Inline quick-add saves + logs without leaving the meal-log flow (DIET-01, D-01).
- Quick-add macros are entered manually with no external food-DB lookup (D-02, D-14).
- Search-as-you-type + Recent + auto-ranked Frequent (DIET-04, D-06).
- One-action copy-a-meal lands editable pending items (DIET-05).
- Live totals incl. net carbs + %-of-target bars with always-present labels (DIET-06).
- History renders snapshots, not re-resolved foods (DIET-09).
- No window.confirm / modals; full suite + production build green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-diet-ux-overhaul/02-04-SUMMARY.md`
</output>
</content>
</invoke>

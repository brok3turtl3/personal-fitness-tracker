---
phase: 02-diet-ux-overhaul
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-03"]
files_modified:
  - src/app/features/charts/charts-page.component.ts
  - src/app/features/charts/charts-page.component.spec.ts
autonomous: true
requirements: [DIET-07, DIET-08]

must_haves:
  truths:
    - "The charts page shows a diet series (calories + macros over time) alongside cardio/weight/readings, honoring the date-range filter (DIET-07)"
    - "Diet macros are separately toggleable via checkbox controls mirroring the cardio pattern"
    - "Diet day-bucketing SUMS per local day via sumByDay keyed on dateTime — multi-meal days are not halved, and a DST-boundary meal keys to the correct local day (DIET-08)"
    - "D-10: the diet calories + macros series live in the EXISTING charts page, reusing the date-range + checkbox controls, with each macro toggleable — no new charts route/page"
    - "D-11: diet day-bucketing uses local-time day boundaries via sumByDay/toDateKey and is DST-tested (23:30-local meal on a spring-forward date keys to the correct local day)"
  artifacts:
    - path: "src/app/features/charts/charts-page.component.ts"
      provides: "Diet calories+macros datasets + controls + DietService injection"
      contains: "sumByDay"
  key_links:
    - from: "charts-page.component.ts buildDietChart"
      to: "src/app/shared/chart-grouping.ts sumByDay"
      via: "local-day summation on MealEntry.dateTime"
      pattern: "sumByDay"
    - from: "charts-page.component.ts"
      to: "src/app/services/diet.service.ts getMealsInRange"
      via: "DietService injection"
      pattern: "getMealsInRange|DietService"
---

<objective>
Integrate diet history into the existing charts page: a calories series (reusing the cardio energy hue) plus separately-toggleable protein/fat/carbs/net-carbs macro series, all driven by the existing date-range filter and `controlsForm` checkbox pattern. Bucket by LOCAL day via the new `sumByDay` (SUMS, keyed on `dateTime`) — never `groupByDay` (which averages and expects `.date`).

Purpose: DIET-07 (diet history in charts with date-range filter, D-10 — reuse the existing page + controls) + DIET-08 (local-time day boundaries consistent with diet/reports, DST-safe, D-11).
Output: extended `charts-page.component.ts` + spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-diet-ux-overhaul/02-UI-SPEC.md
@.planning/phases/02-diet-ux-overhaul/02-RESEARCH.md
@.planning/phases/02-diet-ux-overhaul/02-PATTERNS.md
@src/app/shared/chart-grouping.ts

<interfaces>
<!-- Verified live structure of charts-page.component.ts -->
- Imports `ChartData<'line'>` from chart.js (6); `filterByRange, resolveDateRange` from date-range (25).
- Injects storageService (310), cardioService (311), weightService (312), readingsService (313) — NO DietService yet (ADD it).
- `controlsForm` (267, fb.group at 315) with `cardioShowDistance: [false]` (319), `cardioShowCalories: [false]` (320). Checkbox markup at template 109-113. `(change)="onControlsChanged()"`.
- `cardioChartData / weightChartData / readingsChartData: ChartData<'line'>` (281-283).
- `refresh()` forkJoins `cardioService.getSessions()/weightService.getEntries()/readingsService.getReadings()` (371-373), resolves range (390-394), calls `buildCardioChart(range)` (401).
- `buildCardioChart(range)` (430-515): calories dataset at 476-486 (`borderColor: '#e67e22'`, `backgroundColor: 'rgba(230,126,34,0.12)'`, `pointRadius: 2`, `tension: 0.25`, `yAxisID`), multi-axis `drawOnChartArea: false` at ~500.

DietService (Plan 03): `getMealsInRange(startMs, endMs): Observable<MealEntry[]>`.
sumByDay (Plan 01, chart-grouping.ts): `sumByDay<T>(items, dateOf, extractor): { labels: string[]; values: number[][] }` — SUMS per local day via toDateKey.
</interfaces>

<ui-contract>
<!-- LOCKED chart palette from 02-UI-SPEC.md -->
- Calories (kcal): borderColor #e67e22 (same energy hue as cardio), primary y axis.
- Protein #16a085, Fat #f1c40f, Carbs #8e44ad — macros (g) share a secondary axis (y1) with grid.drawOnChartArea:false.
- Net carbs #c0392b, dashed borderDash:[4,4] (differentiated by dash, not only hue).
- All series: backgroundColor rgba(…,0.12–0.15), pointRadius:2, tension:0.25.
- Macros separately toggleable: add dietShowCalories/dietShowProtein/dietShowFat/dietShowCarbs/dietShowNetCarbs to controlsForm mirroring cardioShow*; checkbox markup mirrors lines 109-113; (change)="onControlsChanged()".
- Charts diet-empty state: reuse <app-empty-state> "No diet data in this range" / "Log some meals or widen the date range to see calories and macros over time."
</ui-contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject DietService + buildDietChart (calories + toggleable macros) via sumByDay + controls (DIET-07/08)</name>
  <files>src/app/features/charts/charts-page.component.ts, src/app/features/charts/charts-page.component.spec.ts</files>
  <read_first>
    - .planning/phases/02-diet-ux-overhaul/02-UI-SPEC.md (chart series palette + toggle pattern + diet-empty copy)
    - .planning/phases/02-diet-ux-overhaul/02-RESEARCH.md §Pattern 5 (213-224) + §Pitfall 2 (279-283 — sum-not-average, do NOT use groupByDay)
    - .planning/phases/02-diet-ux-overhaul/02-PATTERNS.md "charts-page.component.ts" section (216-231) + "chart-grouping.ts" (234-242)
    - src/app/features/charts/charts-page.component.ts lines 267/281-283/310-320/336-401/430-515 (controlsForm, refresh, buildCardioChart template)
    - src/app/shared/chart-grouping.ts sumByDay (Plan 01) + toDateKey
  </read_first>
  <action>
In `src/app/features/charts/charts-page.component.ts`:
- Inject `DietService` in the constructor (alongside cardio/weight/readings).
- Add a `dietChartData: ChartData<'line'> = { labels: [], datasets: [] }` field (mirror cardioChartData at 281-283) + a template chart block + diet-empty `<app-empty-state>` ("No diet data in this range" / the LOCKED body) gated on `dietChartData.labels?.length`. Add this into the EXISTING charts page — do NOT create a new charts route/page (D-10).
- Add controls to `controlsForm` (315-320): `dietShowCalories: [false], dietShowProtein: [false], dietShowFat: [false], dietShowCarbs: [false], dietShowNetCarbs: [false]` — mirror the `cardioShow*` checkbox markup (109-113) with `(change)="onControlsChanged()"` and aria-labels (reuse the existing date-range + checkbox controls — D-10).
- In `refresh()` add `diet: this.dietService.getMealsInRange(range.startMs ?? 0, range.endMs ?? Date.now())` to the forkJoin (or a parallel subscription), then call a new `buildDietChart(meals, range)` (pipe takeUntilDestroyed). Resolve the range exactly as the cardio path does (390-394).
- `buildDietChart(meals, range)`: filter via the resolved range (the getter already range-filters; keep it consistent with `filterByRange` semantics), then bucket with `sumByDay(meals, m => m.dateTime, m => [m.totals.caloriesKcal, m.totals.proteinG, m.totals.fatG, m.totals.carbsG, m.totals.netCarbsG])`. Map the resulting `labels` + `values[]` columns into datasets gated by the corresponding `dietShow*` control, using the LOCKED palette: calories `#e67e22` on primary `y`; protein `#16a085`, fat `#f1c40f`, carbs `#8e44ad`, net carbs `#c0392b` with `borderDash:[4,4]` on the macro/secondary axis (`y1`, `grid.drawOnChartArea:false` — copy the cardio multi-axis treatment at ~500). All series `pointRadius:2`, `tension:0.25`, `backgroundColor` rgba(…,0.12).
- DO NOT call `groupByDay` for diet (it averages + expects `.date`); reuse ONLY `toDateKey` via `sumByDay` for local-time day boundaries (Pitfall 2, D-11).
Extend `src/app/features/charts/charts-page.component.spec.ts`:
- Provide a DietService spy returning meals across multiple days incl. two meals on the SAME local day; assert the diet calories dataset for that day is the SUM (not the average) of the two meals.
- Assert toggling `dietShowProtein` adds/removes the protein dataset.
- Assert a meal logged at 23:30 local on a DST-boundary date (2026-03-08) buckets to the correct local-day label (DIET-08, D-11).
- Inline axe-core via the existing helper, gated serious|critical (carry forward the existing chart.js controller-registration + color-contrast disable conventions used in this spec).
  </action>
  <verify>
    <automated>ng test --no-watch --browsers=ChromeHeadless --include='**/charts-page.component.spec.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'sumByDay' src/app/features/charts/charts-page.component.ts` returns at least 1
    - `grep -c 'groupByDay' src/app/features/charts/charts-page.component.ts` returns 0 (diet does NOT use the averaging helper)
    - `grep -c 'getMealsInRange\|DietService' src/app/features/charts/charts-page.component.ts` returns at least 1
    - `grep -c 'dietShowCalories\|dietShowProtein' src/app/features/charts/charts-page.component.ts` returns at least 1
    - `grep -c '#16a085\|#8e44ad' src/app/features/charts/charts-page.component.ts` returns at least 1 (locked macro hues)
    - charts-page.component.spec asserts SUMMED (not averaged) calories on a same-local-day pair AND a DST-boundary label
    - `ng test --no-watch --browsers=ChromeHeadless --include='**/charts-page.component.spec.ts'` exits 0
  </acceptance_criteria>
  <done>DietService injected; diet calories + toggleable macro series render on the existing charts page via sumByDay (sum-per-local-day, DST-correct) + the date-range filter; locked palette + multi-axis treatment; groupByDay NOT used for diet; spec green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| meal data → chart.js datasets | numeric totals + food-derived labels feed the chart |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering | day-bucketing correctness (DIET-08) | mitigate | sumByDay SUMS (not averages) and keys via toDateKey on dateTime; spec asserts same-day SUM + a DST-boundary label — no UTC drift, no halved macros (Pitfall 2, D-11) |
| T-02-05-02 | Tampering / Elevation | chart label XSS | mitigate | Chart data/labels passed via ng2-charts `[data]` binding (data, not HTML); food names are not rendered as innerHTML; no `bypassSecurityTrust*` introduced (T-XSS — RESEARCH verified) |
| T-02-05-03 | Information disclosure | charts egress | accept | Charts read local meal data via DietService→StorageService; no network call introduced (T-EGRESS upheld) |
</threat_model>

<verification>
- Full suite `ng test --no-watch --browsers=ChromeHeadless` green.
- `ng build --configuration=production` exits 0.
- `grep -rnE "innerHTML|bypassSecurityTrust|fetch\(" src/app/features/charts/charts-page.component.ts` returns no matches (T-XSS / T-EGRESS).
</verification>

<success_criteria>
- Diet calories + macros render on the EXISTING charts page, honoring the date-range filter, macros separately toggleable (DIET-07, D-10).
- Day-bucketing SUMS per local day, DST-correct (DIET-08, D-11).
- Full suite + production build green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-diet-ux-overhaul/02-05-SUMMARY.md`
</output>
</content>
</invoke>

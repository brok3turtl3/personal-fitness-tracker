---
phase: 05-web-search-grounding-quality-sweep
plan: 05
type: execute
wave: 3
depends_on: [02, 04]
files_modified:
  - src/app/services/anthropic-api.service.ts
  - src/app/services/anthropic-api.service.spec.ts
  - src/app/services/chat.service.ts
  - src/app/services/chat.service.spec.ts
  - src/app/services/tool-registry.service.ts
  - src/app/services/tool-registry.service.spec.ts
  - src/app/services/fitness-context.service.ts
  - src/app/services/fitness-context.service.spec.ts
  - src/app/models/ai-chat.model.ts
autonomous: true
requirements: [RESCH-01]
must_haves:
  truths:
    - "web_search_20250305 is appended to tools[] ONLY when enableWebSearch === true, with max_uses === webSearchMaxUses (RESCH-01, D-01, D-06)"
    - "A server_tool_use / web_search_tool_result turn is rendered, NEVER dispatched through ToolRegistryService, and posts back NO tool_result (D-02, E3)"
    - "pause_turn resumes by re-sending the paused turn unmodified without decrementing maxAgentTurns (D-02, Phase 4 D-16)"
    - "isWriteProposal('web_search') === false (read-only server tool, D-02)"
    - "A web_search_tool_result_error renders an honest error event with no retry, no throw (E4, Pitfall 4)"
    - "The system prompt carries the when-to-search (D-08) + query-string privacy (D-10) instructions; web search OFF by default (E5)"
  artifacts:
    - path: "src/app/services/anthropic-api.service.ts"
      provides: "buildWebSearchTool(settings) → WebSearchTool20250305 | null"
      contains: "web_search_20250305"
    - path: "src/app/services/chat.service.ts"
      provides: "server-tool render-only emit loop + pause_turn live path"
      contains: "server_tool_use"
  key_links:
    - from: "src/app/services/chat.service.ts"
      to: "anthropic-api buildWebSearchTool"
      via: "tools[] assembly gated on enableWebSearch"
      pattern: "buildWebSearchTool|enableWebSearch"
    - from: "src/app/services/tool-registry.service.ts"
      to: "WRITE_PROPOSAL_TOOLS"
      via: "web_search NEVER added"
      pattern: "isWriteProposal"
---

<objective>
Wire the `web_search_20250305` server tool into the agentic loop (RESCH-01, D-01/D-02) without rebuilding the loop: (1) `anthropic-api.service.ts` (sole SDK importer) builds the tool def gated on `enableWebSearch` with `max_uses = webSearchMaxUses`; (2) `chat.service.ts` `runAgenticLoop` appends it to the existing filtered `tools[]`, emits live `web_search_started`/`web_search_results`/`web_search_error` `ChatTurnEvent`s for the inline `server_tool_use`/`web_search_tool_result` blocks (render-only — NO dispatch, NO `tool_result` posted back), and the already-present `pause_turn` branch becomes live; (3) `tool-registry.service.ts` keeps `web_search` out of dispatch + `WRITE_PROPOSAL_TOOLS`; (4) `fitness-context.service.ts` adds the D-08 when-to-search + D-10 query-privacy instructions to the cached system prefix; (5) `ai-chat.model.ts` gains the three SDK-agnostic web-search `ChatTurnEvent` variants.

Purpose: This is the behavioral core of web-search grounding (RESCH-01) and the loop-correctness guards (E3/E4). It runs after 05-02 (serializer/model foundation) and 05-04 (which also edits chat.service.ts) to serialize the shared-file edits.
Output: transport + loop + registry + prompt + model wired for the opt-in server tool, with E3/E4/E5/E6 specs over the F1–F14 fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-web-search-grounding-quality-sweep/05-RESEARCH.md
@.planning/phases/05-web-search-grounding-quality-sweep/05-AI-SPEC.md
@.planning/phases/05-web-search-grounding-quality-sweep/05-PATTERNS.md

<interfaces>
<!-- anthropic-api.service.ts: sole SDK importer (D-17). import type block 4-10. sendMessage 69-77, countTokens 90-108, AnthropicApiError 22+, mapError 127-165 (401 at 149-165 preserved) -->
buildWebSearchTool(settings: { enableWebSearch: boolean; webSearchMaxUses: number }): WebSearchTool20250305 | null
  → if (!enableWebSearch) return null; return { type:'web_search_20250305', name:'web_search', max_uses: webSearchMaxUses ?? 3 }
  // allowed_domains/blocked_domains/user_location: UNSET (D-07)

<!-- chat.service.ts runAgenticLoop (297-489): tools filter 312-317 (memory/query_ gates); dispatch list filter 412-419 `b.type === 'tool_use'`; pause_turn branch 384-398 (turn--; continue); MAX_CONSECUTIVE_PAUSES cap 325-326 -->
<!-- AI-SPEC §3 Entry Point Pattern (B): serverToolBlocks = filter b.type==='server_tool_use'; webResultBlocks = filter b.type==='web_search_tool_result'; emit web_search_started/results/error; NO dispatch, NO tool_result -->

<!-- tool-registry.service.ts: WRITE_PROPOSAL_TOOLS Set (67-69), isWriteProposal (87-89), definitions() (131) -->

<!-- fitness-context.service.ts: INSTRUCTIONS (101), GRADING_INSTRUCTIONS (121, has [source: research] at 126), buildSystemPrompt → SystemTextBlock[] (52), redactMealNotes gating (176) -->

<!-- ai-chat.model.ts ChatTurnEvent (127-132) — SDK-agnostic. ADD:
  | { kind:'web_search_started'; query: string }
  | { kind:'web_search_results'; count: number }
  | { kind:'web_search_error'; code: string }
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: buildWebSearchTool at the transport boundary + tools[] assembly + ChatTurnEvent variants</name>
  <files>src/app/services/anthropic-api.service.ts, src/app/services/anthropic-api.service.spec.ts, src/app/models/ai-chat.model.ts, src/app/services/chat.service.ts</files>
  <read_first>
    - src/app/services/anthropic-api.service.ts (import type block 4-10, sendMessage 69-77, countTokens 90-108, D-17 comment)
    - .planning/phases/05-web-search-grounding-quality-sweep/05-AI-SPEC.md (§3 Entry Point Pattern (A) + Core Imports — the SDK types to add)
    - src/app/services/chat.service.ts (tools filter 312-317; how tool defs map to SDK at the boundary)
    - src/app/models/ai-chat.model.ts (ChatTurnEvent union 127-132 — SDK-agnostic convention at line 120)
    - .planning/phases/05-web-search-grounding-quality-sweep/05-PATTERNS.md (anthropic-api + chat.service assignments)
  </read_first>
  <action>
    1. `anthropic-api.service.ts`: extend the `import type { … } from '@anthropic-ai/sdk/resources/messages'` block (4-10) with `WebSearchTool20250305, ServerToolUseBlock, WebSearchToolResultBlock, WebSearchResultBlock, WebSearchToolResultError, WebSearchToolResultErrorCode, CitationsWebSearchResultLocation, TextCitation`. Add a public `buildWebSearchTool(settings: { enableWebSearch: boolean; webSearchMaxUses: number }): WebSearchTool20250305 | null` returning `null` when `!enableWebSearch`, else `{ type:'web_search_20250305', name:'web_search', max_uses: settings.webSearchMaxUses ?? 3 }` (D-01/D-06). Leave `allowed_domains`/`blocked_domains`/`user_location` UNSET (D-07). The 401/`mapError`/`dangerouslyAllowBrowser` paths stay verbatim. Also add a public narrowing method that calls `toGroundedCitations(textBlock.citations)` from `web-citation-parser.ts` while mapping each assistant turn (so SDK citation types never leave this file — D-17); OR confirm the serializer (05-02) already does the narrowing on persist and the renderer reads persisted `GroundedCitation[]` — if so, this method is unnecessary; document which path is used.
    2. `ai-chat.model.ts`: add the three SDK-agnostic `ChatTurnEvent` variants (see `<interfaces>`): `web_search_started` (query:string), `web_search_results` (count:number), `web_search_error` (code:string). Plain string fields only — NO SDK types (D-17).
    3. `chat.service.ts`: in the `tools` assembly (312-317), after the existing memory/query_ filter, append the web-search tool def when `toolSettings.enableWebSearch` — built at the transport boundary via `anthropicApi.buildWebSearchTool(toolSettings)` and pushed onto the same array (per AI-SPEC §3 (A): `...(webSearchTool ? [webSearchTool] : [])`). The SDK-typed def is produced in anthropic-api; chat.service holds it via the local structural shape it already uses for tool defs (keep chat.service's TYPE-ONLY SDK boundary intact). Ensure the web-search tool def is included in the `countTokens` tools array so its schema cost is counted (AI-SPEC §4 Context Window).
  </action>
  <verify>
    <automated>ng test --no-watch --include='**/anthropic-api.service.spec.ts' 2>&1 | tail -3; grep -q "web_search_20250305" src/app/services/anthropic-api.service.ts && grep -q "web_search_started" src/app/models/ai-chat.model.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "buildWebSearchTool" src/app/services/anthropic-api.service.ts` ≥ 1
    - `grep -c "web_search_20250305" src/app/services/anthropic-api.service.ts` ≥ 1
    - `! grep -nE "allowed_domains|blocked_domains|user_location" src/app/services/anthropic-api.service.ts` (D-07 — unset)
    - `grep -c "web_search_started\|web_search_results\|web_search_error" src/app/models/ai-chat.model.ts` ≥ 3
    - `! grep -nE "@anthropic-ai/sdk" src/app/models/ai-chat.model.ts` AND the only SDK import in chat.service.ts is `import type` (D-17 boundary intact — grep gate)
    - a spec asserts `buildWebSearchTool({enableWebSearch:false,...})` returns null and `{enableWebSearch:true, webSearchMaxUses:5}` returns `max_uses === 5` (E6)
  </acceptance_criteria>
  <done>The opt-in server-tool def is built at the D-17 boundary with the configured cap, appended to tools[] only when enabled, and the three live web-search events exist SDK-free.</done>
</task>

<task type="auto">
  <name>Task 2: Loop render-only server-tool handling + pause_turn live + registry read-only guard (E3/E4)</name>
  <files>src/app/services/chat.service.ts, src/app/services/chat.service.spec.ts, src/app/services/tool-registry.service.ts, src/app/services/tool-registry.service.spec.ts</files>
  <read_first>
    - src/app/services/chat.service.ts (runAgenticLoop 297-489, dispatch filter 412-419, pause_turn branch 384-398, terminal switch 373-409)
    - src/app/services/tool-registry.service.ts (WRITE_PROPOSAL_TOOLS 67-69, isWriteProposal 87-89, definitions 131)
    - .planning/phases/05-web-search-grounding-quality-sweep/05-AI-SPEC.md (§3 Entry Point Pattern (B) — the exact emit loop; Common Pitfalls 1/2/6)
    - src/app/services/web-citation-parser.fixtures.ts (F9_PAUSED/F9_RESUME, F10, F11, F12 from 05-01)
    - src/app/services/chat.service.spec.ts + tool-registry.service.spec.ts (existing spec patterns)
  </read_first>
  <action>
    1. `chat.service.ts`: after each awaited `Message`, add a SEPARATE filter (NOT widening the `b.type === 'tool_use'` dispatch filter at 412-419) for `server_tool_use` and `web_search_tool_result` blocks (AI-SPEC §3 (B)): for each `server_tool_use` emit `{ kind:'web_search_started', query: (input as {query?:string}).query ?? '' }`; for each `web_search_tool_result` narrow `content` — if `Array.isArray(content)` emit `{ kind:'web_search_results', count: content.length }`, else emit `{ kind:'web_search_error', code: content.error_code }` (HTTP-200 error union — Pitfall 4; NO retry, NO throw). These are render-only: NO `ToolRegistryService.dispatch`, NO `tool_result` posted back (server tool already ran). The assistant turn (incl. server-tool blocks) is pushed back unmodified for the next turn (the serializer from 05-02 preserves encrypted fields). The existing `pause_turn` branch (384-398, `turn--; continue;`) becomes live — verify it pushes the paused assistant turn unmodified and does NOT count against `maxAgentTurns` (the `MAX_CONSECUTIVE_PAUSES` cap stays). A web-search turn ending `end_turn` exits via the existing arm (no new branch).
    2. `tool-registry.service.ts`: confirm/assert `web_search` is NEVER in `WRITE_PROPOSAL_TOOLS` (read-only — `isWriteProposal('web_search') === false`) and is NEVER registered as a dispatchable executor (server tools have no client executor — D-02). Add a clarifying comment at the allow-list (57-66). No functional change expected; add the guarantee as a spec.
    3. Specs (consume F9/F10/F11/F12): `chat.service.spec.ts` — a `server_tool_use`+`web_search_tool_result` turn (F10) produces ZERO `dispatch` calls (spy) and posts ZERO `tool_result`; emits `web_search_started` + `web_search_results`. A `pause_turn` sequence (F9_PAUSED→F9_RESUME) resumes by re-sending the paused turn unmodified and does NOT decrement `maxAgentTurns` (assert the resume call args include the paused assistant turn; assert turn count). An error fixture (F11 `max_uses_exceeded`, F12 `too_many_requests`/`query_too_long`) emits `web_search_error` with the code, issues NO re-call (spy on sendMessage call count), throws nothing. `tool-registry.service.spec.ts` — `isWriteProposal('web_search')` is false; a server-tool turn yields zero dispatch.
  </action>
  <verify>
    <automated>ng test --no-watch --include='**/chat.service.spec.ts' 2>&1 | tail -5; ng test --no-watch --include='**/tool-registry.service.spec.ts' 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "server_tool_use" src/app/services/chat.service.ts` ≥ 1 (the render-only filter)
    - the dispatch filter `b.type === 'tool_use'` at ~412-419 is UNCHANGED (not widened to include server_tool_use)
    - `! grep -n "'web_search'" src/app/services/tool-registry.service.ts | grep -i "WRITE_PROPOSAL"` (web_search never in the write allow-list)
    - a chat spec asserts a server-tool turn → 0 dispatch calls, 0 tool_result posted (E3)
    - a chat spec asserts pause_turn resume re-sends the paused turn unmodified, no maxAgentTurns decrement (E3)
    - a chat spec asserts each error code → web_search_error event, 0 re-calls, no throw (E4)
    - a registry spec asserts isWriteProposal('web_search') === false
    - both specs pass
  </acceptance_criteria>
  <done>Server-tool turns render via live events and are never dispatched or answered with a tool_result; pause_turn is live and turn-safe; web-search is read-only and error-resilient (E3/E4).</done>
</task>

<task type="auto">
  <name>Task 3: System-prompt when-to-search (D-08) + query-string privacy (D-10) instructions</name>
  <files>src/app/services/fitness-context.service.ts, src/app/services/fitness-context.service.spec.ts</files>
  <read_first>
    - src/app/services/fitness-context.service.ts (INSTRUCTIONS 101, GRADING_INSTRUCTIONS 121-126, buildSystemPrompt 52, byte-stable-prefix doc-comment 52-70, redactMealNotes 176)
    - .planning/phases/05-web-search-grounding-quality-sweep/05-AI-SPEC.md (§4b Prompt Engineering Discipline — the EXACT three instruction strings + the 1-2 few-shot exemplars)
    - src/app/services/fitness-context.service.spec.ts (E7 byte-stable prefix spec pattern)
    - src/app/services/web-citation-parser.fixtures.ts (F14_PII_BAIT_PROMPT for the E5 reference)
  </read_first>
  <action>
    Extend the cacheable `INSTRUCTIONS` prefix (101) — STABLE text in the cached prefix, NOT volatile per-turn data (preserve byte-stability discipline 52-70) — with the three AI-SPEC §4b instruction strings (use the AI-SPEC wording verbatim):
    (1) WHEN TO SEARCH (D-08): "prefer the user's own logged data and your training knowledge; reach for web_search for genuinely current or research-grounded questions — recent studies, current guidelines — not for questions you can answer from training knowledge or the user's data."
    (2) GROUNDED-VS-UNGROUNDED FRAMING (D-04): "when a claim is backed by a live web source, it will carry a citation; an un-grounded 'from research' claim is your training knowledge — label it as such and do not present it as a live source."
    (3) QUERY-STRING PRIVACY (D-10): "do NOT put the user's personal or health specifics, or their per-entry free-text notes, into web-search query strings; phrase searches in general terms (the query leaves the device)."
    Add the 1-2 short inline few-shot exemplars from AI-SPEC §4b (a general PII-free query example + a grounded vs un-grounded example) in the cached prefix; do NOT dynamically retrieve. Confirm `redactMealNotes` (176) gates per-entry free-text notes (per-note granularity, NOT the aggregate macro line — D-10) — leave its existing behavior; document the confirmation. Web search is OFF by default (the def is only built when enabled — Task 1) — no change to the default needed; the instruction text is always present (harmless when web search is off, and stable for caching).
    Spec (E5): `fitness-context.service.spec.ts` — assert the built system prompt text contains the D-10 query-privacy instruction substring and the D-08 when-to-search substring; assert the cacheable prefix stays byte-stable across two builds (extend the existing E7 spec — adding stable instruction text must not break byte stability).
  </action>
  <verify>
    <automated>ng test --no-watch --include='**/fitness-context.service.spec.ts' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "web_search\|web search" src/app/services/fitness-context.service.ts` ≥ 1 (the new instructions)
    - a spec asserts the system prompt contains the D-10 privacy substring (e.g. "leaves the device" or the exact AI-SPEC phrase) and the D-08 when-to-search substring
    - the existing byte-stable-prefix (E7) spec still passes (instructions are in the cached, not the volatile, block)
    - `! grep -nE "@anthropic-ai/sdk" src/app/services/fitness-context.service.ts` (D-17 — stays SDK-free)
    - `fitness-context.service.spec.ts` passes
  </acceptance_criteria>
  <done>The cached system prompt carries the when-to-search, grounded-vs-un-grounded, and query-privacy instructions; web search stays OFF by default; the cacheable prefix remains byte-stable (E5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Anthropic API | The model-authored web-search query string egresses the device — the new outbound surface |
| server_tool_use block → loop | A block named `web_search` must never be executed as a client tool (double-bill/wedge) |
| SDK types → services/models | D-17: web-search SDK types confined to anthropic-api.service.ts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Information Disclosure | web-search query string | mitigate | D-10 system-prompt instruction discourages PII/note free-text in queries; OFF by default; per-note redaction gating preserved (best-effort behavioral guard — E5 spot-check backstop) |
| T-05-05-02 | DoS / cost | server_tool_use dispatch | mitigate | Dispatch filter `b.type==='tool_use'` unchanged; server_tool_use render-only; web_search never in WRITE_PROPOSAL_TOOLS or dispatch table; no tool_result round-trip (E3, Pitfall 1) |
| T-05-05-03 | DoS / cost | pause_turn loop | mitigate | pause_turn resumes unmodified, turn-- (no maxAgentTurns evasion), MAX_CONSECUTIVE_PAUSES cap intact (E3, Pitfall 2) |
| T-05-05-04 | DoS | web_search_tool_result_error (HTTP 200) | mitigate | content narrowed against the error union; honest web_search_error event; NO retry (re-bill), NO throw/crash (E4, Pitfall 4/6) |
| T-05-05-05 | Information Disclosure | SDK type leak | mitigate | SDK types confined to anthropic-api.service.ts; chat.service type-only boundary; model + fitness-context SDK-free (grep gates — D-17) |
| T-05-05-06 | Spoofing | unbounded cost | mitigate | max_uses = webSearchMaxUses (default 3) wired to the request (E6); maxAgentTurns bounds the loop |
</threat_model>

<verification>
- anthropic-api / chat.service / tool-registry / fitness-context specs all pass
- D-17 grep gates green (SDK confined to anthropic-api.service.ts)
- Full Karma suite green; `ng build --configuration=production` exits 0
</verification>

<success_criteria>
The opt-in web_search server tool is wired into the loop end-to-end: built with the configured cap at the D-17 boundary, rendered (never dispatched) via live events, pause_turn-safe, error-resilient, and steered by the when-to-search + privacy system prompt — RESCH-01 complete at the service layer (renderer is 05-08).
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-search-grounding-quality-sweep/05-05-SUMMARY.md`
</output>

---
phase: 05-web-search-grounding-quality-sweep
plan: 04
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/app/services/storage.service.ts
  - src/app/services/storage.service.spec.ts
  - src/app/services/chat.service.ts
  - src/app/services/chat.service.spec.ts
autonomous: true
requirements: [QUAL-02, QUAL-04, QUAL-05]
must_haves:
  truths:
    - "StorageService reports real usage % via navigator.storage.estimate() instead of a hardcoded 5MB (QUAL-02)"
    - "saveData detects QuotaExceededError AND Firefox NS_ERROR_DOM_QUOTA_REACHED (QUAL-02, D-14)"
    - "lastModified is readable for multi-tab change comparison (QUAL-04)"
    - "Pre-summary chat messages move to a lazy per-conversation archive key through StorageService and load on demand (QUAL-05, D-13)"
  artifacts:
    - path: "src/app/services/storage.service.ts"
      provides: "estimate()-driven quota, cross-browser quota-error matching, archive-key methods, lastModified accessor"
      contains: "navigator.storage"
  key_links:
    - from: "src/app/services/chat.service.ts"
      to: "StorageService archive methods"
      via: "pre-summary message move on summarization"
      pattern: "archive"
    - from: "src/app/services/storage.service.ts"
      to: "fitness_tracker_archive_"
      via: "per-conversation archive key prefix"
      pattern: "fitness_tracker_archive_"
---

<objective>
The storage-layer half of the quality sweep's safety features (all behind the Phase 1 chokepoint — zero `localStorage.*` outside `storage.service.ts`): (1) QUAL-02 quota detection via `navigator.storage.estimate()` replacing the hardcoded 5 MB, plus cross-browser quota-error matching (`QuotaExceededError` + Firefox `NS_ERROR_DOM_QUOTA_REACHED` + numeric codes); (2) QUAL-04 expose `lastModified` for multi-tab comparison; (3) QUAL-05/D-13 lazy per-conversation chat archival keys (`fitness_tracker_archive_{conversationId}`) with on-demand load, and the `ChatService` summarization path that moves pre-summary messages into them.

Purpose: This is the data-plane work the app-level banners (05-07) and the chat archival affordance (05-08) consume. It runs after 05-02 (V6 migration) because both modify `storage.service.ts`.
Output: `storage.service.ts` + spec with estimate-driven quota, cross-browser error matching, archive-key methods, and `lastModified` accessor; `chat.service.ts` + spec moving pre-summary messages to the archive on summarization.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-web-search-grounding-quality-sweep/05-RESEARCH.md
@.planning/phases/05-web-search-grounding-quality-sweep/05-PATTERNS.md

<interfaces>
<!-- storage.service.ts analog points -->
getStorageInfo (308-330): hardcoded `const estimatedTotal = 5 * 1024 * 1024` at 314  // REPLACE with estimate()
saveData (262-278): writes lastModified (264-265); catch matches only `e.name === 'QuotaExceededError'` (272)  // EXTEND matching
writeBackup (172-181) / getBackup (200-206) / pruneOldBackups (216-235) / setDevSeed/consumeDevSeed (351-380)  // template for new archive keys
StorageInfo interface — extend with usagePct?: number

<!-- RESEARCH §Pattern 4 -->
async readQuota(): { pct } | null  // navigator.storage?.estimate() guard → (usage/quota)*100, null on old browsers
isQuotaError(e): DOMException && (name 'QuotaExceededError' | 'NS_ERROR_DOM_QUOTA_REACHED' | code 22 | code 1014)

<!-- D-13 archival: ChatConversation already has summary + summarizedMessageCount fields -->
archive key prefix: static readonly ARCHIVE_KEY_PREFIX = 'fitness_tracker_archive_'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quota detection (estimate + cross-browser error matching) + lastModified accessor</name>
  <files>src/app/services/storage.service.ts, src/app/services/storage.service.spec.ts</files>
  <read_first>
    - src/app/services/storage.service.ts (getStorageInfo 308-330, saveData 262-278, lastModified write 264-265, StorageInfo interface)
    - src/app/services/storage.service.spec.ts (existing mock pattern for localStorage + DOMException)
    - .planning/phases/05-web-search-grounding-quality-sweep/05-RESEARCH.md (§Pattern 4 — exact readQuota + isQuotaError bodies + the origin-wide caveat)
  </read_first>
  <action>
    1. Replace the hardcoded `5 * 1024 * 1024` in `getStorageInfo` (314) with a `navigator.storage.estimate()`-driven usage percentage. Add a private async-style helper (wrap in an Observable to fit the existing `Observable<StorageInfo>` return) per RESEARCH §Pattern 4: guard `if (!navigator.storage?.estimate) return null` (graceful null on old browsers), compute `(usage/quota)*100`. Extend the `StorageInfo` interface with `usagePct?: number` (undefined when estimate unavailable — the consumer falls back to the existing byte-count display). Keep the existing fields for backward compat. Note in a comment the origin-wide caveat (estimate is origin-wide, not LocalStorage-only — the honest proactive signal; write-time error matching is the reactive backstop).
    2. Extend `saveData`'s catch (272) to a shared private `isQuotaError(e: unknown): boolean` matching `DOMException && (e.name === 'QuotaExceededError' || e.name === 'NS_ERROR_DOM_QUOTA_REACHED' || (e as DOMException).code === 22 || (e as DOMException).code === 1014)` (RESEARCH §Pattern 4). On a quota error, throw the existing `StorageError` with a quota-specific code/message the UI (05-07) keys on (e.g. `StorageErrorCode.QUOTA_EXCEEDED` — add it to the enum if absent). Non-quota errors keep the existing path.
    3. Add a public `getLastModified(): string | null` accessor reading the cached/stored `lastModified` (QUAL-04 — it is written at 264-265 but never read). This is the value 05-07's multi-tab listener compares against an incoming `storage` event.
    4. Extend `storage.service.spec.ts`: spec the estimate path (mock `navigator.storage.estimate` → 70%, 95%, and the unavailable→null case); spec `isQuotaError` against a `QuotaExceededError` DOMException, a Firefox-named one, and a non-quota error; spec `getLastModified` returns the stored value.
  </action>
  <verify>
    <automated>ng test --no-watch --include='**/storage.service.spec.ts' 2>&1 | tail -5; grep -q "NS_ERROR_DOM_QUOTA_REACHED" src/app/services/storage.service.ts && grep -q "navigator.storage" src/app/services/storage.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -n "5 \* 1024 \* 1024" src/app/services/storage.service.ts` (hardcoded 5MB removed)
    - `grep -c "navigator.storage" src/app/services/storage.service.ts` ≥ 1
    - `grep -c "NS_ERROR_DOM_QUOTA_REACHED" src/app/services/storage.service.ts` ≥ 1 AND code 22/1014 matched
    - `grep -c "getLastModified" src/app/services/storage.service.ts` ≥ 1
    - specs assert: estimate → usagePct at 70/95, estimate-unavailable → undefined/null, isQuotaError true for both browser names + false for a generic error
    - `storage.service.spec.ts` passes
  </acceptance_criteria>
  <done>Quota is read from navigator.storage.estimate() (no hardcoded 5MB), both browser quota-error names + numeric codes are matched on write, and lastModified is readable — all behind the chokepoint.</done>
</task>

<task type="auto">
  <name>Task 2: Lazy per-conversation chat archival keys + summarization move</name>
  <files>src/app/services/storage.service.ts, src/app/services/storage.service.spec.ts, src/app/services/chat.service.ts, src/app/services/chat.service.spec.ts</files>
  <read_first>
    - src/app/services/storage.service.ts (writeBackup 172-181, getBackup 200-206, pruneOldBackups 216-235, setDevSeed/consumeDevSeed 351-380 — the sanctioned new-key pattern)
    - src/app/services/chat.service.ts (the summarization path — search for `summary`/`summarizedMessageCount`/`summarize`)
    - src/app/models/ai-chat.model.ts (ChatConversation summary + summarizedMessageCount fields)
    - src/app/services/chat.service.spec.ts (existing spec pattern)
    - .planning/phases/05-web-search-grounding-quality-sweep/05-PATTERNS.md (storage archival assignment — D-13)
  </read_first>
  <action>
    1. Add archive-key methods to `StorageService` keyed `fitness_tracker_archive_{conversationId}` (D-13), copying the `writeBackup`/`getBackup`/`consumeDevSeed` sanctioned pattern (best-effort try/catch, `static readonly ARCHIVE_KEY_PREFIX = 'fitness_tracker_archive_'`, never-throw read):
       - `archiveMessages(conversationId: string, messages: ChatMessage[]): void` — appends the given pre-summary messages to the per-conversation archive key (read existing archived JSON, prepend/append, write back). Best-effort.
       - `loadArchivedMessages(conversationId: string): ChatMessage[]` — reads + parses the archive key; returns `[]` on absent/parse-error (never throws — matches `getBackup`).
       - `hasArchivedMessages(conversationId: string): boolean` — cheap presence check for the UI affordance (05-08).
       ALL through `localStorage` calls INSIDE storage.service.ts only (chokepoint).
    2. In `chat.service.ts`, at the summarization point (where messages are summarized and `summarizedMessageCount` advances), MOVE the pre-summary messages out of the hot `AppData` conversation slice into the archive key via `storage.archiveMessages(...)` before trimming them from the active slice — so the active conversation stays small (cap-pressure relief, D-13). The summary text + `summarizedMessageCount` stay on the active conversation; only the raw pre-summary messages move. Nothing is deleted (archival is a move).
    3. Specs: `storage.service.spec.ts` — archive round-trip (archive → load returns the messages; absent → `[]`; malformed → `[]`, no throw; key uses the `fitness_tracker_archive_` prefix). `chat.service.spec.ts` — on summarization, pre-summary messages are passed to `archiveMessages` and removed from the active slice while the summary is retained.
  </action>
  <verify>
    <automated>ng test --no-watch --include='**/storage.service.spec.ts' 2>&1 | tail -3; ng test --no-watch --include='**/chat.service.spec.ts' 2>&1 | tail -3; grep -q "fitness_tracker_archive_" src/app/services/storage.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fitness_tracker_archive_" src/app/services/storage.service.ts` ≥ 1 (key prefix constant)
    - `grep -c "archiveMessages\|loadArchivedMessages\|hasArchivedMessages" src/app/services/storage.service.ts` ≥ 3
    - `! grep -rnE "localStorage\.(getItem|setItem|removeItem)" src/app/services/chat.service.ts` (chokepoint — chat.service never touches localStorage directly)
    - a storage spec asserts archive→load round-trip + absent→[] + malformed→[] (no throw)
    - a chat spec asserts summarization moves pre-summary messages to the archive and shrinks the active slice while keeping the summary
    - both specs pass
  </acceptance_criteria>
  <done>Pre-summary messages lazily archive to per-conversation keys through StorageService and load on demand; the active conversation slice stays small; the chokepoint stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser quota API → app | estimate()/error names vary by browser; must degrade gracefully, never crash a write |
| archive keys → LocalStorage | new keys must route through the chokepoint; archived data must not be lost or corrupted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | DoS | saveData quota path | mitigate | Cross-browser quota-error matching (both names + codes 22/1014) → typed StorageError; the 95% block (05-07) prevents silent write loss |
| T-05-04-02 | DoS | navigator.storage.estimate | mitigate | `navigator.storage?.estimate` guard returns null on old browsers; write-time error matching is the reactive backstop (RESEARCH §Pattern 4 caveat) |
| T-05-04-03 | Tampering | archive keys | mitigate | All archive access inside storage.service.ts (chokepoint grep gate); loads never throw (parse-error → []), archival is a move not a delete (no data loss) |
| T-05-04-04 | Information Disclosure | lastModified accessor | accept | lastModified is a non-sensitive ISO timestamp; exposing it for multi-tab comparison reveals nothing private |
</threat_model>

<verification>
- storage + chat specs pass under `ng test --no-watch`
- Chokepoint grep gate green (no localStorage outside storage.service.ts)
- `ng build --configuration=production` exits 0
</verification>

<success_criteria>
The data plane supports real quota detection, cross-browser quota-error handling, multi-tab change comparison, and lazy chat archival — all behind the storage chokepoint, ready for the app-level banners (05-07) and the archival affordance (05-08).
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-search-grounding-quality-sweep/05-04-SUMMARY.md`
</output>

---
phase: 05-web-search-grounding-quality-sweep
plan: 08
type: execute
wave: 4
depends_on: [02, 04, 05]
files_modified:
  - src/app/features/chat/chat-message-list.component.ts
  - src/app/features/chat/chat-message-list.component.spec.ts
  - src/app/features/settings/settings-ai.component.ts
  - src/app/features/settings/settings-ai.component.spec.ts
autonomous: true
requirements: [RESCH-02, RESCH-03, QUAL-05]
must_haves:
  truths:
    - "A grounded claim renders an inline footnote [n] (accent https: link) + a per-message Sources list; un-grounded research stays plain text with no link, visibly distinct by the · grounded suffix + marker (RESCH-02, D-03/D-04)"
    - "A live web-search row renders 🔎 Searching… → 🔎 Found {n} sources, expandable (D-05)"
    - "Across the OFF/ON matrix, model-authored prose citation strings yield ZERO <a>; only structured web_search_result_location https: blocks become links (RESCH-03, E1, D-09)"
    - "A user sees 'Load earlier messages' when an archive key exists and lazy-loads through StorageService (QUAL-05, D-13)"
    - "webSearchMaxUses cost-context helper line is visible near the toggle in /settings/ai (D-06)"
  artifacts:
    - path: "src/app/features/chat/chat-message-list.component.ts"
      provides: "footnote markers + Sources list + grounded badge + live search row + archival affordance"
      contains: "Sources"
  key_links:
    - from: "src/app/features/chat/chat-message-list.component.ts"
      to: "isLinkableCitation allow-list"
      via: "ONLY structured web_search_result_location https: → <a>"
      pattern: "isLinkableCitation"
    - from: "src/app/features/chat/chat-message-list.component.ts"
      to: "StorageService.loadArchivedMessages"
      via: "Load earlier messages affordance"
      pattern: "loadArchived|Load earlier"
---

<objective>
The web-search grounding renderer + the E1 adversarial acceptance gate + the chat-archival affordance + the settings cost helper: (1) extend the Phase 4 `@switch` renderer with the live `🔎` web-search row (D-05), inline footnote markers `[n]` + a per-message **Sources** list (https-only, D-03), and the `research · grounded` source-badge upgrade (D-04) — un-grounded research stays plain text (Phase 4 framing preserved); (2) the RESCH-03/E1 adversarial spec proving zero `<a>` from model prose across the web-OFF/ON matrix (the single most important eval artifact, zero tolerance); (3) the QUAL-05/D-13 "Load earlier messages" affordance lazy-loading archived messages through `StorageService`; (4) the D-06 `webSearchMaxUses` cost-context helper line in `/settings/ai`.

Purpose: This is where grounding becomes visible and where the no-fabricated-link guarantee is enforced and tested. It runs after 05-02 (GroundedCitation/serializer), 05-04 (archive load), and 05-05 (live web-search events).
Output: the extended renderer + E1 adversarial spec over F1–F8, the archival affordance, and the settings cost helper, all keeping color never the only channel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-web-search-grounding-quality-sweep/05-UI-SPEC.md
@.planning/phases/05-web-search-grounding-quality-sweep/05-AI-SPEC.md
@.planning/phases/05-web-search-grounding-quality-sweep/05-PATTERNS.md

<interfaces>
<!-- chat-message-list.component.ts: isLinkableCitation allow-list 21-26 (admits web_search_result_location); @switch 52-92; 'text' case 54 (confidence-chip/source-chip, research qualifier 'general knowledge — not a live source'); spansFor memo 296-304 + spanCache 276; confidenceGlyph 312-316; isCalm 307-309; message-content wrapper (Phase 1 characterization contract — PRESERVE) -->
<!-- GroundedCitation { url; title; citedText } persisted on TextBlock.citations (05-02); the model TextBlock now carries citations -->
<!-- 05-04 StorageService: loadArchivedMessages(convId), hasArchivedMessages(convId) -->
<!-- 05-05 ChatTurnEvent: web_search_started/results/error (the live row driver) -->

<!-- LOCKED UI-SPEC copy (Web-search live progress + grounding) -->
in-flight: `🔎 Searching the web…` (role=status aria-live=polite, @keyframes blink, non-expandable)
in-flight w/ query: `🔎 Searching the web for "{query}"…`
resolved summary: `🔎 Found {n} sources`
disclosure summary accessible name: `Show what the AI searched for: {summary}`
disclosure heading `Web search`; query label `Searched:`; results label `Sources found:`
Sources heading: `Sources`; item `[{n}] {title}` with https: URL as link
footnote marker: `[{n}]` accent link → #source-{msgId}-{n}; accessible name `Source {n}: {title}`; 14px/600 baseline (NOT <sup>)
grounded badge label: `research · grounded`; icon 📚🔗; accessible name `Source: from research, grounded in {n} live web source(s)`
un-grounded research badge (UNCHANGED): label `research`; qualifier `general knowledge — not a live source`
{n}/{title}/{query}/URL renderer-derived from structured blocks — NEVER model-free-authored; omit token if unavailable; only https: → link
archival: `Load earlier messages` (accessible name `Load earlier archived messages in this conversation`); loading `Loading earlier messages…`

<!-- settings-ai.component.ts: enableWebSearch toggle 145 (aria-describedby enable-search-helper), webSearchMaxUses input 164-170 (min=0 max=10) -->
<!-- LOCKED D-06 copy: webSearchMaxUses cost-context helper (NEW near field): `Each web search adds to the cost of a message. This caps how many the AI can run per request.` -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grounded footnotes + Sources list + grounded badge + live search row</name>
  <files>src/app/features/chat/chat-message-list.component.ts, src/app/features/chat/chat-message-list.component.spec.ts</files>
  <read_first>
    - src/app/features/chat/chat-message-list.component.ts (isLinkableCitation 21-26, @switch 52-92, 'text' case 54, spansFor/spanCache memo 276-304, confidenceGlyph/isCalm, message-content wrapper)
    - .planning/phases/05-web-search-grounding-quality-sweep/05-UI-SPEC.md (Copywriting Contract web-search section, Component Inventory web-search rows + footnote + Sources, Color source-badge palette D-04, Typography footnote rules, Accessibility Contract rows, Layout in-stream render order)
    - src/app/services/web-citation-parser.ts (toGroundedCitations/toSourcesList — 05-02)
    - src/app/models/ai-chat.model.ts (TextBlock.citations + server-tool block variants — 05-02; ChatTurnEvent web_search_* — 05-05)
  </read_first>
  <action>
    Extend the Phase 4 `@switch (block.type)` renderer (PRESERVE the `<div class="message-content">` wrapper — Phase 1 characterization contract):
    1. Live web-search row (D-05) — add a branch for the persisted `server_tool_use`/`web_search_tool_result` blocks (and the live `web_search_started`/`web_search_results`/`web_search_error` events from 05-05) reusing the Phase 4 tool-disclosure treatment: in-flight present-tense `🔎 Searching the web…` (`role="status"` `aria-live="polite"`, existing `@keyframes blink`, non-expandable, 44px); resolved collapsed `<details>` `🔎 Found {n} sources` with summary accessible name `Show what the AI searched for: {summary}`, body `Web search` heading + `Searched:` query + `Sources found:` list. An error result renders an honest `🔎 Couldn't reach the web` row (no link). Search rows sit in execution order above the prose (Layout order).
    2. Grounded footnotes (D-03) — in the `'text'` case, for a claim whose `TextBlock.citations` (narrowed `GroundedCitation[]` via `toGroundedCitations`, memoized the SAME way as `spansFor`/`spanCache` — parse once per block/text key, NEVER in a CD-rebound binding) yield ≥1 grounded citation, render an inline footnote marker `<a href="#source-{msgId}-{n}">[{n}]</a>` at 14px/600 accent baseline (NOT `<sup>`), accessible name `Source {n}: {title}`. Foot the message with a per-message `<section aria-label="Sources">` Sources list (only when ≥1 grounded citation): `Sources` heading + ordered items `id="source-{msgId}-{n}"`, title (600) + the `https:` URL as a native `<a>` (accent). Use `toSourcesList` dedupe. Only `https:` → link; a non-https url in a structured block renders plain text (gate already in the parser — 05-02).
    3. Grounded source badge (D-04) — upgrade the `'text'` source-axis (line 54): when a claim is backed by a grounded citation, render the source chip with `· grounded` label suffix + `📚🔗` icon + the `[n]` footnote marker, accessible name per UI-SPEC. An un-grounded "from research" claim KEEPS the exact Phase 4 plain-text framing (`research` + `general knowledge — not a live source`, zero links). Distinguished by label suffix + marker presence + icon — NEVER color alone (both share the neutral `#f0f0f0` chip).
    4. CRITICAL GUARD (D-09): the ONLY path to an `<a href>` is via the `isLinkableCitation` allow-list (21-26) on a structured `web_search_result_location` GroundedCitation, https-gated. Model prose stays inert via `{{ }}` auto-escape (no `innerHTML`, no `bypassSecurityTrust*`, no markdown autolinking). Confidence/source confidence-chip glyph+label pattern stays the non-color channel.
    5. Extend the component spec with DOM assertions: F2 grounded turn → footnote `[1]` anchor + Sources list with the https link + `research · grounded` badge; F4 un-grounded → NO link, NO grounded badge, the un-grounded qualifier present; F5 mixed → both render distinguishably; F6 non-https → plain text no link; F7 null-title → host fallback; live row renders for a web_search event; `expectNoSeriousA11yViolations` (color-contrast enforced).
  </action>
  <verify>
    <automated>ng test --no-watch --include='**/chat-message-list.component.spec.ts' 2>&1 | tail -5; ! grep -nE "bypassSecurityTrust|innerHTML" src/app/features/chat/chat-message-list.component.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Sources\|Searching the web\|Found" src/app/features/chat/chat-message-list.component.ts` ≥ 3 (LOCKED copy)
    - `grep -c "research · grounded\|· grounded" src/app/features/chat/chat-message-list.component.ts` ≥ 1 (D-04 badge)
    - `! grep -nE "bypassSecurityTrust|innerHTML" src/app/features/chat/chat-message-list.component.ts` (no XSS path)
    - the `message-content` wrapper is preserved (`grep -c "message-content" …` unchanged from Phase 4)
    - specs assert F2 grounded → footnote anchor + Sources https link + grounded badge; F4 un-grounded → 0 links + qualifier present; F6 non-https → plain text
    - grounded-citation narrowing is memoized (a `…Cache` Map keyed by msgId/idx/text — like spanCache)
    - a11y (color-contrast) clean; spec passes
  </acceptance_criteria>
  <done>Grounded claims render footnotes + a Sources list + the · grounded badge (https-only links); un-grounded research stays plain Phase-4 text; the live search row renders; color is never the only channel.</done>
</task>

<task type="auto">
  <name>Task 2: E1 adversarial citation-link guard spec (RESCH-03, zero tolerance)</name>
  <files>src/app/features/chat/chat-message-list.component.spec.ts</files>
  <read_first>
    - src/app/features/chat/chat-message-list.component.spec.ts (the Phase 4 isLinkableCitation guard test — E1 extends it)
    - .planning/phases/05-web-search-grounding-quality-sweep/05-AI-SPEC.md (§5 E1 rubric + F1/F3 fixtures + §6 online guardrail; the zero-tolerance hard-CI rule)
    - src/app/services/web-citation-parser.fixtures.ts (F1 web-OFF prose, F3 web-ON prose+real block, F2 grounded — from 05-01)
    - .planning/phases/05-web-search-grounding-quality-sweep/05-UI-SPEC.md (Citation guard D-09 render rule — both axes)
  </read_first>
  <action>
    Add the E1 adversarial regression spec to `chat-message-list.component.spec.ts` — the RESCH-03 acceptance gate, zero tolerance (AI-SPEC §5 E1 + §6). Render fixtures and assert on `querySelectorAll('a')`:
    - WEB-OFF axis (F1): a message whose `text` contains the model-authored prose `Smith et al. 2021` + a bare DOI `10.1234/abcd`, NO citations → `querySelectorAll('a').length === 0` (the entire message, for the model-authored prose claim).
    - WEB-ON axis (F3): a message with ONE real grounded `web_search_result_location` block PLUS a model-authored `Jones 2020` prose string → the ONLY anchors trace to the structured grounded citation; the prose author-year string is NOT an anchor; assert the anchor count equals the grounded-citation count and the anchor href is `https:`.
    - non-https structured block (F6) → NOT an anchor (plain text).
    - assert `toGroundedCitations` never emits a link from a non-`web_search_result_location` citation (direct unit assertion alongside the DOM assertion).
    Add a JSDoc/describe-block comment: "E1 — RESCH-03 adversarial citation-link integrity, ZERO TOLERANCE: any fabricated/non-https link is a build-failing red, not a warning (AI-SPEC §5/§6)."
  </action>
  <verify>
    <automated>ng test --no-watch --include='**/chat-message-list.component.spec.ts' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - a spec asserts F1 (web-OFF prose) → `querySelectorAll('a').length === 0`
    - a spec asserts F3 (web-ON prose + 1 real block) → anchor count === grounded-citation count, prose author-year NOT an anchor, anchor href starts `https:`
    - a spec asserts F6 non-https structured block → not an anchor
    - a spec asserts `toGroundedCitations` drops a non-web_search_result_location citation
    - the describe block is labeled as the E1 zero-tolerance adversarial gate
    - all E1 specs pass under `ng test --no-watch`
  </acceptance_criteria>
  <done>The E1 adversarial gate proves, across the web-OFF/ON matrix, that only structured https web_search_result_location blocks become links and model prose never does — RESCH-03 enforced and zero-tolerance.</done>
</task>

<task type="auto">
  <name>Task 3: Load-earlier archival affordance (QUAL-05/D-13) + settings webSearchMaxUses cost helper (D-06)</name>
  <files>src/app/features/chat/chat-message-list.component.ts, src/app/features/chat/chat-message-list.component.spec.ts, src/app/features/settings/settings-ai.component.ts, src/app/features/settings/settings-ai.component.spec.ts</files>
  <read_first>
    - src/app/features/chat/chat-message-list.component.ts (top-of-stream insertion point; takeUntilDestroyed pattern)
    - src/app/services/storage.service.ts (loadArchivedMessages / hasArchivedMessages — 05-04)
    - src/app/features/settings/settings-ai.component.ts (enableWebSearch toggle 145, webSearchMaxUses field 164-170, existing aria-describedby helper pattern, form styles)
    - .planning/phases/05-web-search-grounding-quality-sweep/05-UI-SPEC.md (Chat archival copy + a11y row; Web-search settings cost context copy D-06)
    - src/app/features/settings/settings-ai.component.spec.ts
  </read_first>
  <action>
    1. Archival affordance (QUAL-05/D-13): at the TOP of the active chat stream, when `StorageService.hasArchivedMessages(conversationId)` is true, render a native `<button>` `Load earlier messages` (accessible name `Load earlier archived messages in this conversation`). On click → `loadArchivedMessages(conversationId)` (lazy, through StorageService — 05-04), prepend the returned messages to the rendered list, announce `Loading earlier messages…` via `aria-live="polite"`, and preserve the scroll anchor (do NOT jump the user to the top abruptly — UI-SPEC a11y). Subscribe via `takeUntilDestroyed`. Archival is a non-destructive move (no confirm).
    2. Settings cost helper (D-06): in `settings-ai.component.ts`, add the LOCKED cost-context helper line near the `webSearchMaxUses` field (164-170) using the existing `aria-describedby` helper pattern (copy the `enable-search-helper` style at 145): `Each web search adds to the cost of a message. This caps how many the AI can run per request.` The field stays `min=0 max=10` (already wired — D-06 confirms the bound; no new control). The value visibly bounds cost by sitting adjacent to the enable toggle (success-criterion 1).
    3. Specs: chat-message-list spec — when `hasArchivedMessages` true, the `Load earlier messages` button renders; clicking calls `loadArchivedMessages` and prepends; when false, no button; a11y clean. settings-ai spec — the cost-context helper text is present in the DOM near the webSearchMaxUses field; the field bound stays min=0 max=10; a11y clean.
  </action>
  <verify>
    <automated>ng test --no-watch --include='**/chat-message-list.component.spec.ts' 2>&1 | tail -3; ng test --no-watch --include='**/settings-ai.component.spec.ts' 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Load earlier messages" src/app/features/chat/chat-message-list.component.ts` ≥ 1 (LOCKED copy)
    - `grep -c "loadArchivedMessages\|hasArchivedMessages" src/app/features/chat/chat-message-list.component.ts` ≥ 2
    - `grep -c "Each web search adds to the cost" src/app/features/settings/settings-ai.component.ts` ≥ 1 (LOCKED D-06 helper)
    - `grep -q 'max="10"' src/app/features/settings/settings-ai.component.ts` (bound confirmed)
    - a chat spec asserts the affordance renders only when archived messages exist + clicking lazy-loads + prepends
    - a settings spec asserts the cost helper is present near the field
    - both specs pass; a11y clean
  </acceptance_criteria>
  <done>The user can lazy-load archived messages through StorageService, and the webSearchMaxUses cap is surfaced with cost context near the toggle — QUAL-05 UI + D-06 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model output → DOM | Model prose + citations are untrusted; only structured https citations may become links |
| archive key → rendered stream | Archived messages loaded from storage are rendered; same render rules apply |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | Spoofing / Tampering | citation rendering | mitigate | ONLY isLinkableCitation web_search_result_location https: GroundedCitation → `<a>`; model prose inert via `{{ }}` auto-escape; E1 zero-tolerance spec asserts `querySelectorAll('a').length===0` for prose across OFF/ON (RESCH-03, Pitfall 2/3) |
| T-05-08-02 | Tampering | XSS via model output | mitigate | No `innerHTML`/`bypassSecurityTrust*`/markdown autolinking (grep gate); strict interpolation only |
| T-05-08-03 | Spoofing | grounded/un-grounded confusion | mitigate | D-04 grounded vs un-grounded distinguished by `· grounded` suffix + `[n]` marker + icon, not color alone (E2); compliance-required Sources list always rendered for grounded claims (Pitfall 3) |
| T-05-08-04 | Tampering | archived message rendering | mitigate | Archived messages render through the same guarded `@switch` (no separate unsafe path); loaded read-only through StorageService chokepoint |
| T-05-08-05 | Information Disclosure | renderer-derived tokens | mitigate | {n}/{title}/{query}/URL renderer-derived from structured blocks, never model-free-authored (UI-SPEC E12); token omitted if unavailable, never guessed |
</threat_model>

<verification>
- chat-message-list spec (incl. E1 zero-tolerance) + settings-ai spec pass
- Phase 1 chat characterization spec stays green (message-content wrapper preserved)
- No `innerHTML`/`bypassSecurityTrust*` in the renderer
- a11y (color-contrast) clean; `ng build --configuration=production` exits 0
</verification>

<success_criteria>
Grounded web claims render as honest, https-only, structured-citation-backed footnotes + Sources list, visibly distinct from un-grounded research; the E1 adversarial gate proves no fabricated links across the matrix; archived messages lazy-load; the cost cap is surfaced — RESCH-02/03 + QUAL-05 UI + D-06 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-search-grounding-quality-sweep/05-08-SUMMARY.md`
</output>

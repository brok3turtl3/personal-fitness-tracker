---
phase: 05-web-search-grounding-quality-sweep
plan: 07
type: execute
wave: 3
depends_on: [04]
files_modified:
  - src/app/app.component.ts
  - src/app/app.component.html
  - src/app/app.component.css
  - src/app/app.component.spec.ts
  - src/app/shared/quota-banner.component.ts
  - src/app/shared/quota-banner.component.spec.ts
  - src/app/shared/multi-tab-banner.component.ts
  - src/app/shared/multi-tab-banner.component.spec.ts
  - src/index.html
  - e2e/csp.spec.mjs
autonomous: true
requirements: [QUAL-02, QUAL-04, QUAL-06]
must_haves:
  truths:
    - "A user at ≥70% storage sees a dismissible warn banner; at ≥95% a persistent block banner offering archive/delete + copy-JSON (QUAL-02, D-14)"
    - "A user editing in one window sees the other surface a 'data changed elsewhere — refresh' banner via the storage event (QUAL-04)"
    - "src/index.html carries the CSP meta tag; connect-src allows only https://api.anthropic.com; styles still load (QUAL-06, A1)"
    - "Banners announce via aria-live and never steal focus; color + icon + text triple-encode each state (UI-SPEC a11y)"
  artifacts:
    - path: "src/index.html"
      provides: "CSP meta tag"
      contains: "Content-Security-Policy"
    - path: "src/app/shared/quota-banner.component.ts"
      provides: "70% warn / 95% block banner composing <app-error-state> voice"
      contains: "Manage storage"
  key_links:
    - from: "src/app/app.component.ts"
      to: "window storage event"
      via: "multi-tab listener comparing lastModified"
      pattern: "addEventListener\\('storage'|storage"
    - from: "src/app/app.component.ts"
      to: "StorageService quota read"
      via: "quota banner threshold check"
      pattern: "getStorageInfo|usagePct"
---

<objective>
The app-level UI for the quality sweep's safety features: (1) QUAL-02 quota banners — a ≥70% dismissible warn banner and a ≥95% persistent block banner (archive/delete + the `StorageService.getBackup` copy-JSON safety valve, NO export — D-14), driven by the `navigator.storage.estimate()` data from 05-04; (2) QUAL-04 a `storage`-event `window` listener comparing the incoming `lastModified` against `StorageService.getLastModified()` and raising a neutral "data changed elsewhere — refresh" banner; (3) QUAL-06 the CSP `<meta>` tag in `src/index.html` (with the A1 `style-src 'self' 'unsafe-inline'` addition for Angular inline styles) + an e2e console-error check.

Purpose: Surfaces the 05-04 data-plane safety features and locks the egress allow-list. Banners are hosted ahead of `<router-outlet>` beside the existing recovery banner; the planner may collapse the stack to one-at-a-time by severity.
Output: two new banner components, app.component wiring, the CSP meta tag + e2e assertion, and specs (incl. synthetic StorageEvent — Pitfall 6).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-web-search-grounding-quality-sweep/05-UI-SPEC.md
@.planning/phases/05-web-search-grounding-quality-sweep/05-RESEARCH.md
@.planning/phases/05-web-search-grounding-quality-sweep/05-PATTERNS.md

<interfaces>
<!-- app.component.ts: recovery-banner host pattern 32-99; loadBackupJson chokepoint comment 71-81; destroyRef 29; init subscribe takeUntilDestroyed 37-38 -->
<!-- recovery-banner.component.ts: thin <app-error-state> wrapper 24-55; clipboard/feature-detect 76-88 — STRUCTURAL TEMPLATE for new banners -->
<!-- 05-04 StorageService surface: getStorageInfo() → StorageInfo{ usagePct? }, getLastModified(): string|null, getBackup(key) (existing clipboard JSON), StorageErrorCode.QUOTA_EXCEEDED -->

<!-- LOCKED UI-SPEC copy -->
70% warn: title `Storage is filling up`; body `You've used about {pct}% of this app's local storage. Consider archiving old chats or deleting entries you no longer need.`; dismiss `Dismiss`; amber #fdf3e7/#b9770e + ⚠ ; role=status aria-live=polite; dismissible
95% block: title `Storage is full — new entries can't be saved`; body `You've used about {pct}% of local storage, so saving is paused. Free up space by archiving chat history or deleting old entries.`; primary `Manage storage`; safety-valve `Copy all data as JSON`; red #fdeaea/#c0392b + ⛔ ; role=alert aria-live=assertive; persistent
multi-tab: title `Data changed in another window`; body `This app is open in more than one place and the data changed elsewhere. Refresh to see the latest.`; actions `Refresh` + `Dismiss`; neutral #f8f9fa/#2c3e50/#ddd + 🔄 ; role=status aria-live=polite

<!-- CSP policy (RESEARCH §Pattern 5 + A1) -->
<meta http-equiv="Content-Security-Policy" content="default-src 'self'; connect-src 'self' https://api.anthropic.com; script-src 'self'; object-src 'none'; img-src 'self' data:; style-src 'self' 'unsafe-inline';">

<!-- banner stacking order (UI-SPEC Layout): recovery > 95% block > multi-tab > 70% warn; planner may collapse to most-severe-only -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quota banner + multi-tab banner components + app.component host wiring</name>
  <files>src/app/shared/quota-banner.component.ts, src/app/shared/quota-banner.component.spec.ts, src/app/shared/multi-tab-banner.component.ts, src/app/shared/multi-tab-banner.component.spec.ts, src/app/app.component.ts, src/app/app.component.html, src/app/app.component.css, src/app/app.component.spec.ts</files>
  <read_first>
    - src/app/app.component.ts + app.component.html (recovery-banner host 32-99, destroyRef, init subscribe)
    - src/app/shared/recovery-banner.component.ts (the thin <app-error-state> wrapper + clipboard feature-detect — the structural template)
    - src/app/services/storage.service.ts (the 05-04 surface: getStorageInfo/usagePct, getLastModified, getBackup, StorageErrorCode.QUOTA_EXCEEDED)
    - .planning/phases/05-web-search-grounding-quality-sweep/05-UI-SPEC.md (Quota/Multi-tab banner copy + visual treatment + Focal Point + Accessibility Contract rows + banner stacking)
    - .planning/phases/05-web-search-grounding-quality-sweep/05-RESEARCH.md (Pitfall 6 — storage event fires only in OTHER tabs; test with synthetic StorageEvent)
  </read_first>
  <action>
    1. Create `src/app/shared/quota-banner.component.ts` — a standalone component composing the `<app-error-state>` voice (recovery-banner template), with two modes: `warn` (≥70%, amber #fdf3e7/#b9770e, `⚠`, `role="status"` `aria-live="polite"`, dismissible, LOCKED warn copy + `{pct}` token) and `block` (≥95%, red #fdeaea/#c0392b, `⛔`, `role="alert"` `aria-live="assertive"`, persistent, LOCKED block copy + `Manage storage` primary action routing to where delete/archive lives + `Copy all data as JSON` safety valve reusing the clipboard feature-detect from recovery-banner against `StorageService.getBackup`). Inputs: `mode`, `pct`; outputs: `dismiss`, `manageStorage`, `copyJson`. Styles component-scoped (NO new global CSS — UI-SPEC); color + icon + text triple-encode each state.
    2. Create `src/app/shared/multi-tab-banner.component.ts` — neutral informational banner (`#f8f9fa`/`#2c3e50`/`#ddd`, `🔄`, `role="status"` `aria-live="polite"`, NOT error-colored), LOCKED copy, `Refresh` + `Dismiss` actions. Refresh triggers a reload of the active data (re-fetch through services / `window.location.reload()` is acceptable for a local app — choose the least surprising; document).
    3. `app.component.ts`/`.html`/`.css`: host both banners ahead of `<router-outlet>` beside the recovery banner. Quota: on init + after writes, read `StorageService.getStorageInfo()` `usagePct`; show warn at ≥70, block at ≥95 (block is persistent while ≥95). Add a `window` `storage` event listener (registered in the constructor/ngOnInit, removed via `destroyRef.onDestroy`) that, on a `StorageEvent` for the app's data key whose `newValue`'s `lastModified` differs from `StorageService.getLastModified()`, raises the multi-tab banner (Pitfall 6 — the event only fires in OTHER tabs; compare lastModified to avoid false positives from unrelated keys). All data access through `StorageService` (chokepoint — NO direct `localStorage.*` in app.component). Pipe subscribes through `takeUntilDestroyed(this.destroyRef)`. Implement banner stacking by severity per UI-SPEC (recovery > 95% block > multi-tab > 70% warn) — collapsing to the single most-severe applicable banner is acceptable (document the choice). Banners NEVER steal focus.
    4. Specs: `quota-banner.component.spec.ts` (warn vs block render the LOCKED copy + role + icon; copyJson invokes the clipboard path; `expectNoSeriousA11yViolations`). `multi-tab-banner.component.spec.ts` (LOCKED copy + neutral styling + Refresh/Dismiss; a11y). `app.component.spec.ts` — dispatch a SYNTHETIC `StorageEvent` (Pitfall 6) with a differing lastModified → multi-tab banner appears; same lastModified → no banner; mock `getStorageInfo` at 70/95 → warn/block banner appears; banners do not call `.focus()`.
  </action>
  <verify>
    <automated>ng test --no-watch --include='**/quota-banner.component.spec.ts' 2>&1 | tail -3; ng test --no-watch --include='**/multi-tab-banner.component.spec.ts' 2>&1 | tail -3; ng test --no-watch --include='**/app.component.spec.ts' 2>&1 | tail -3; ! grep -nE "localStorage\.(getItem|setItem|removeItem)" src/app/app.component.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Storage is filling up\|Storage is full" src/app/shared/quota-banner.component.ts` ≥ 2 (LOCKED titles)
    - `grep -c "Manage storage\|Copy all data as JSON" src/app/shared/quota-banner.component.ts` ≥ 2
    - `grep -c "Data changed in another window" src/app/shared/multi-tab-banner.component.ts` ≥ 1
    - `grep -cE "addEventListener\('storage'|'storage'" src/app/app.component.ts` ≥ 1 (multi-tab listener)
    - `! grep -nE "localStorage\.(getItem|setItem|removeItem)" src/app/app.component.ts` (chokepoint green)
    - app.component spec asserts: synthetic StorageEvent with differing lastModified → multi-tab banner; 70/95 usagePct → warn/block banner; no focus steal
    - all three specs pass + a11y (color-contrast) clean
  </acceptance_criteria>
  <done>Quota (70/95) + multi-tab banners are hosted app-level with LOCKED copy/a11y, driven through the StorageService chokepoint, never steal focus, and pass the synthetic-StorageEvent test (Pitfall 6).</done>
</task>

<task type="auto">
  <name>Task 2: CSP meta tag in index.html + e2e console-error check (QUAL-06, A1)</name>
  <files>src/index.html, e2e/csp.spec.mjs</files>
  <read_first>
    - src/index.html (the <head> block 3-9 — insertion point)
    - .planning/phases/05-web-search-grounding-quality-sweep/05-RESEARCH.md (§Pattern 5 + Pitfall 5 + A1 — the style-src 'unsafe-inline' need for Angular inline component styles)
    - src/index.html.csp-assertion.spec.ts (the contract spec from 05-01 — the meta must match it)
    - e2e/run.mjs + e2e/smoke.spec.mjs (the existing Puppeteer harness — pageerror/console.error gate pattern)
  </read_first>
  <action>
    1. Add the CSP `<meta http-equiv="Content-Security-Policy" content="…">` to `src/index.html` `<head>` (3-9) with the EXACT policy in `<interfaces>`: `default-src 'self'; connect-src 'self' https://api.anthropic.com; script-src 'self'; object-src 'none'; img-src 'self' data:; style-src 'self' 'unsafe-inline';`. The `connect-src` is the egress allow-list (Anthropic API as sole external path — locked architecture). A1: include `style-src 'self' 'unsafe-inline'` because Angular 18 injects component `styles:[...]` inline at runtime (Pitfall 5) — verify at build (Task verify) that the app loads styled with NO `Refused to apply inline style` CSP console errors; if styles still break, document the minimal additional directive needed (do NOT loosen `connect-src`). It must satisfy the 05-01 contract spec.
    2. Add `e2e/csp.spec.mjs` to the Puppeteer harness: load the app, assert the CSP meta tag is present in the DOM with the expected `connect-src`/`object-src`/`style-src` directives, and assert ZERO CSP-violation console errors (`Refused to ...`) on a basic route load. Wire it into `e2e/run.mjs` alongside the existing smoke/a11y specs. (e2e runs in the two-terminal flow — operator-verified; the spec is wired but not executed in CI by the executor.)
    3. Run `ng build --configuration=production` and confirm exit 0 with the CSP present. Run `ng serve` is NOT required of the executor; the A1 styled-load verification is done via the e2e spec note + the build gate. Confirm the 05-01 `index.html.csp-assertion.spec.ts` now matches the committed meta tag.
  </action>
  <verify>
    <automated>grep -q "Content-Security-Policy" src/index.html && grep -q "connect-src 'self' https://api.anthropic.com" src/index.html && grep -q "object-src 'none'" src/index.html && grep -q "style-src 'self' 'unsafe-inline'" src/index.html && node --check e2e/csp.spec.mjs && ng test --no-watch --include='**/index.html.csp-assertion.spec.ts' 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Content-Security-Policy" src/index.html` === 1
    - `grep -q "connect-src 'self' https://api.anthropic.com" src/index.html` (egress allow-list — Anthropic only)
    - `grep -q "object-src 'none'" src/index.html` AND `grep -q "style-src 'self' 'unsafe-inline'" src/index.html` (A1)
    - `! grep -E "connect-src[^;]*(\*| http:)" src/index.html` (no wildcard/insecure connect-src)
    - `e2e/csp.spec.mjs` passes `node --check` and is wired into `e2e/run.mjs`
    - the 05-01 CSP assertion spec passes against the committed meta tag
    - `ng build --configuration=production` exits 0; SUMMARY records the A1 styled-load verification outcome
  </acceptance_criteria>
  <done>The CSP meta tag locks external connections to https://api.anthropic.com only, allows Angular inline styles (A1), satisfies the 05-01 contract, and has an e2e console-error check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → external network | CSP connect-src is the egress allow-list — only api.anthropic.com permitted |
| other tab → this tab | A storage event from another tab is untrusted state-change notification |
| quota write failure → user | A blocked write must be surfaced, not silently dropped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Information Disclosure | CSP connect-src | mitigate | `connect-src 'self' https://api.anthropic.com` blocks any other outbound connection; `object-src 'none'`; e2e console-error check verifies enforcement (QUAL-06) |
| T-05-07-02 | DoS | 95% quota block | mitigate | Persistent block banner + StorageError on write surface the full state; archive/delete + copy-JSON safety valve give a path out without losing data (D-14) |
| T-05-07-03 | Tampering | multi-tab last-write-wins | mitigate | storage-event listener compares lastModified and prompts Refresh; the user is warned before stale overwrite (QUAL-04, Pitfall 6) |
| T-05-07-04 | Tampering | CSP style-src 'unsafe-inline' | accept | Required for Angular runtime inline component styles (no server for a nonce); scoped to styles only — script-src stays 'self' (no inline script). Documented A1 deviation |
| T-05-07-05 | Tampering | banner storage access | mitigate | All quota/lastModified/backup access through StorageService (chokepoint grep gate green in app.component) |
</threat_model>

<verification>
- quota-banner, multi-tab-banner, app.component specs pass (incl. synthetic StorageEvent + color-contrast)
- CSP meta present + 05-01 assertion spec green + e2e csp spec node-check
- Chokepoint grep gate green in app.component
- `ng build --configuration=production` exits 0
</verification>

<success_criteria>
The user sees honest quota warnings/blocks and multi-tab change notices, and the app's only permitted external connection is the Anthropic API — QUAL-02/04/06 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-search-grounding-quality-sweep/05-07-SUMMARY.md`
</output>

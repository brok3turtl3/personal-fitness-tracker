---
phase: 05-web-search-grounding-quality-sweep
plan: 09
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05, 06, 07, 08]
files_modified:
  - src/app/features/chat/chat-page.component.ts
  - src/app/features/chat/chat-page.component.spec.ts
  - stryker.config.json
autonomous: false
requirements: [QUAL-01, QUAL-07, QUAL-08, QUAL-09, QUAL-10]
must_haves:
  truths:
    - "A stale 401 from Anthropic surfaces a 'rotate / re-enter key' prompt with a 'Go to settings' action, not a console error (QUAL-07)"
    - "Chat approve/discard/edit failures surface via <app-error-state> instead of console.error-only (QUAL-09, folded IN-01)"
    - "No `any` remains in Phase 5 production paths; any documented exception carries a // TODO rationale (QUAL-01)"
    - "Mutation testing on validators.ts meets a pinned score floor (QUAL-10)"
    - "All 8 feature pages pass axe-core with zero serious/critical violations incl. color-contrast; manual keyboard + contrast pass recorded (QUAL-08)"
  artifacts:
    - path: "src/app/features/chat/chat-page.component.ts"
      provides: "401 rotate-key <app-error-state> + block-action error surfacing"
      contains: "Go to settings"
    - path: "stryker.config.json"
      provides: "pinned mutation-score threshold on validators.ts"
      contains: "thresholds"
  key_links:
    - from: "src/app/features/chat/chat-page.component.ts"
      to: "AnthropicApiError.statusCode === 401"
      via: "rotate-key error-state branch"
      pattern: "401"
---

<objective>
The final quality-sweep closeout: (1) QUAL-07 upgrade the chat-page 401 path to a specific "rotate / re-enter key" `<app-error-state>` prompt with a `Go to settings` action (not console-only); (2) QUAL-09 + folded IN-01 surface chat approve/discard/edit failures via `<app-error-state>` instead of `console.error`-only; (3) QUAL-01 type-tightening sweep — zero `any` in Phase 5 production paths (document any unavoidable third-party exception with `// TODO`); (4) QUAL-10 pin the mutation-score floor on `validators.ts` after measuring baseline; (5) QUAL-08 the final accessibility pass — confirm all 8 feature pages pass axe-core with zero serious/critical (color-contrast now enforced) plus the manual keyboard + manual contrast review checkpoint.

Purpose: Closes the last cross-cutting QUAL items and the milestone quality bar. Runs last (depends on all prior plans) so the a11y/type/mutation gates cover all Phase 5 code.
Output: chat-page 401 + block-action error surfacing + specs, the QUAL-01 sweep, the pinned Stryker floor, and a human-verify a11y checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-web-search-grounding-quality-sweep/05-UI-SPEC.md
@.planning/phases/05-web-search-grounding-quality-sweep/05-RESEARCH.md
@.planning/phases/05-web-search-grounding-quality-sweep/05-PATTERNS.md

<interfaces>
<!-- chat-page.component.ts: handleLoopError 607-619 (maps AnthropicApiError statusCode===401 to inline errorMessage; comment 602-605 "Phase 5 owns rotate-key UX"); onBlockAction 325-392 (console.error at 353/356/390); <app-error-state> Retry surface 75-85; loopError flag pattern -->
<!-- LOCKED UI-SPEC 401 copy: title `Your API key was rejected`; body `Anthropic returned a 401. Your key may be expired, revoked, or mistyped. Re-enter it in settings, then try again.`; primary `Go to settings` (→ /settings/ai); secondary `Retry` -->
<!-- LOCKED UI-SPEC block-action error copy: title `That action didn't go through`; body `We couldn't {action} just now. Try again.` ({action} ∈ save this to memory / discard this / apply this edit) -->
<!-- QUAL-10: stryker.config.json from 05-01 (no thresholds yet). Add `"thresholds": { "high": …, "low": …, "break": <measured-floor> }` after measuring baseline -->
<!-- 8 feature pages: cardio, weight, readings, diet, charts, reports, chat, settings -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 401 rotate-key prompt + block-action error surfacing (QUAL-07 + folded IN-01/QUAL-09)</name>
  <files>src/app/features/chat/chat-page.component.ts, src/app/features/chat/chat-page.component.spec.ts</files>
  <read_first>
    - src/app/features/chat/chat-page.component.ts (handleLoopError 607-619, onBlockAction 325-392 with console.error at 353/356/390, <app-error-state> Retry surface 75-85, AnthropicApiError import)
    - .planning/phases/05-web-search-grounding-quality-sweep/05-UI-SPEC.md (401 API-key rotation copy, Block-action errors copy, 401 Focal Point, Accessibility "401 rotate-key prompt" row)
    - src/app/shared/error-state.component.ts (the <app-error-state> inputs/outputs — title/body/actions, role=alert)
    - src/app/features/chat/chat-page.component.spec.ts
  </read_first>
  <action>
    1. QUAL-07: in `handleLoopError` (607-619), when `err instanceof AnthropicApiError && err.statusCode === 401`, surface a specific rotate-key `<app-error-state>` with the LOCKED copy (title `Your API key was rejected`, body `Anthropic returned a 401. Your key may be expired, revoked, or mistyped. Re-enter it in settings, then try again.`), a primary `Go to settings` action routing to `/settings/ai` (Router), and the existing secondary `Retry`. `role="alert"` `aria-live="assertive"` (reused from `<app-error-state>`); `Go to settings` is the focal point + keyboard-reachable. Resolve the 602-605 "Phase 5 owns rotate-key UX" comment. Non-401 transport errors keep the existing generic recoverable error-state.
    2. QUAL-09 + IN-01: in `onBlockAction` (325-392), replace each `error: (err) => console.error(...)` (353, 356, 390) with surfacing via the existing `errorMessage`/`<app-error-state>` banner using the LOCKED block-action copy (title `That action didn't go through`, body `We couldn't {action} just now. Try again.` with `{action}` ∈ `save this to memory` / `discard this` / `apply this edit` matched to the failing action). Keep a local `console.error` for dev diagnostics IF desired, but the USER-VISIBLE surface is now `<app-error-state>` (the gap IN-01 flagged). 
    3. Specs: chat-page spec — a loop error with `statusCode === 401` renders the rotate-key error-state with `Go to settings` (asserts the action routes to /settings/ai); a generic transport error keeps the generic error-state; each block-action failure (approve/discard/edit error) surfaces the LOCKED `<app-error-state>` copy (not console-only); `expectNoSeriousA11yViolations` (color-contrast) on the 401 + block-action surfaces.
  </action>
  <verify>
    <automated>ng test --no-watch --include='**/chat-page.component.spec.ts' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Your API key was rejected\|Go to settings" src/app/features/chat/chat-page.component.ts` ≥ 2 (LOCKED 401 copy)
    - `grep -c "That action didn't go through" src/app/features/chat/chat-page.component.ts` ≥ 1 (LOCKED block-action copy)
    - the three `console.error`-only block-action handlers (353/356/390) now ALSO surface `<app-error-state>` (user-visible) — spec-asserted
    - a spec asserts statusCode===401 → rotate-key error-state with Go to settings routing to /settings/ai
    - a spec asserts a block-action failure surfaces the LOCKED error-state copy (not console-only)
    - a11y (color-contrast) clean on both surfaces; chat-page spec passes; Phase 1 chat characterization stays green
  </acceptance_criteria>
  <done>A 401 surfaces a rotate-key prompt with a settings path, and chat block-action failures are user-visible via <app-error-state> — QUAL-07 + folded IN-01/QUAL-09 closed.</done>
</task>

<task type="auto">
  <name>Task 2: QUAL-01 type-tightening sweep + QUAL-10 pin mutation-score floor</name>
  <files>stryker.config.json (+ Phase 5 production .ts files, sweep-wide)</files>
  <read_first>
    - .planning/phases/05-web-search-grounding-quality-sweep/05-RESEARCH.md (QUAL-01 risk area = web-search casts at transport boundary; QUAL-10 baseline-then-floor recommendation ~80% on validators.ts)
    - stryker.config.json (from 05-01 — no thresholds yet)
    - all Phase 5 production files touched in 05-02..05-08 (grep for `any`)
    - .planning/phases/05-web-search-grounding-quality-sweep/05-PATTERNS.md (CONVENTIONS no-any rule)
  </read_first>
  <action>
    1. QUAL-01: grep all Phase 5-modified production `.ts` files (web-citation-parser, chat-block-serializer, ai-chat.model, app-data.model, anthropic-api, chat.service, tool-registry, fitness-context, storage.service, cardio/weight/readings services + pages, chat-message-list, chat-page, settings-ai, app.component, the new banner components) for the `any` keyword. Eliminate each `any` in a production path (the web-search SDK casts at the transport boundary are the flagged risk — narrow them with the proper SDK types instead). For any genuinely unavoidable third-party-interface `any`, leave it with a `// TODO: type when lib supports it` rationale per CLAUDE.md. Run `ng build --configuration=production` (strict) to confirm no type regressions.
    2. QUAL-10: run `npx stryker run` to measure the BASELINE mutation score on `validators.ts`. Pin the floor in `stryker.config.json` as a `"thresholds": { "high": <baseline-or-higher>, "low": <floor>, "break": <floor> }` where `break` = the measured baseline score (so the suite fails below it), targeting ≥80% if the baseline supports it (RESEARCH recommendation: set the floor at the measured score, then ratchet — do NOT set an aspirational floor that breaks immediately). Document the measured baseline + the pinned floor in the SUMMARY. Add an npm note that `npm run mutation` enforces the floor.
  </action>
  <verify>
    <automated>ng build --configuration=production 2>&1 | tail -3; grep -q '"thresholds"' stryker.config.json && grep -q '"break"' stryker.config.json</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -rnE "\bany\b" src/app/services/web-citation-parser.ts src/app/services/chat-block-serializer.ts src/app/models/ai-chat.model.ts` (zero any in the new web-search core; OR each match has an adjacent `// TODO: type when` rationale)
    - a tree-wide grep of Phase 5-touched production files shows every remaining `any` carries a `// TODO: type when` comment (recorded in SUMMARY)
    - `stryker.config.json` has a `"thresholds"` block with `"break"` set to the measured baseline floor (not 0, not aspirational)
    - `npx stryker run` passes at/above the pinned floor (SUMMARY records the measured score)
    - `ng build --configuration=production` exits 0
  </acceptance_criteria>
  <done>Phase 5 production paths are `any`-free (documented exceptions only), and the validators.ts mutation-score floor is pinned at the measured baseline so coverage represents real confidence — QUAL-01 + QUAL-10 closed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: QUAL-08 final accessibility pass — automated gate + manual keyboard/contrast review</name>
  <files>src/styles.css (failing-contrast hex nudges only, within LOCKED palette intent), Phase 5 spec files (a11y assertions)</files>
  <read_first>
    - .planning/phases/05-web-search-grounding-quality-sweep/05-UI-SPEC.md (§Color contrast goals, §Accessibility Contract, QUAL-08 deferral-lifted note)
    - .planning/phases/05-web-search-grounding-quality-sweep/05-01-SUMMARY.md (any contrast failures recorded when the deferral was lifted)
  </read_first>
  <action>
    Before pausing for the human review: run the full Karma suite (`ng test --no-watch --code-coverage`) and the e2e a11y harness, and confirm ZERO serious/critical axe violations including color-contrast (the deferral was lifted in 05-01) across all 8 feature pages + the new badge/banner/footnote/search-row surfaces. Fix any contrast failure (recorded by 05-01 or surfaced now) by nudging ONLY the offending hex within the LOCKED palette intent (UI-SPEC §Color: confirm `#2e7d32`-on-`#eef6ec` and `#b9770e`-on-`#fdf3e7` clear WCAG AA; nudge a failing row only, never the calm/alert tier split or icon+label pairing). Then pause for the manual keyboard + manual contrast review below (QUAL-08 mandates a human pass automation cannot fully cover).
  </action>
  <what-built>
    The automated half of QUAL-08 is enforced by code: every Phase 5 spec asserts `expectNoSeriousA11yViolations` with the color-contrast deferral lifted (05-01), across all 8 feature pages + the new badge/banner/footnote surfaces. Before this checkpoint, the executor runs the full suite + the e2e a11y harness and confirms zero serious/critical axe violations (incl. color-contrast) across cardio, weight, readings, diet, charts, reports, chat, settings. Any contrast failure surfaced by lifting the deferral (recorded by 05-01) is fixed here by nudging the offending hex within the LOCKED palette intent (UI-SPEC §Color: confirm `#2e7d32`-on-`#eef6ec` and `#b9770e`-on-`#fdf3e7` clear WCAG AA; nudge only a failing row).
  </what-built>
  <how-to-verify>
    QUAL-08 mandates a MANUAL keyboard + manual contrast review that automation cannot fully cover. With `ng serve` running:
    1. Tab through each of the 8 feature pages — confirm every interactive control (forms, per-row Edit/Save/Discard buttons, footnote markers, Sources links, banner actions, web-search disclosures, Load-earlier button, 401 Go-to-settings) is reachable in DOM order and operable with Enter/Space; no keyboard trap.
    2. Confirm focus management: entering edit mode focuses the first field; Discard returns focus to the Edit button; banners never steal focus.
    3. Visually review color contrast on the confidence/source badges, the grounded `· grounded` badge, the quota banners (amber/red), and the multi-tab banner — confirm each state is distinguishable WITHOUT relying on color (icon + text present) and that text clears AA.
    4. Confirm the live `🔎 Searching the web…` row announces via screen reader (aria-live) if a screen reader is available.
    Report any violations found; otherwise approve.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the a11y/contrast issues found (the executor will fix failing-row hex within the LOCKED palette intent and re-run).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anthropic 401 → user | A rejected key must lead the user to rotate it, not vanish into the console |
| failed block action → user | A swallowed approve/discard/edit failure leaves the user with no feedback |
| test coverage → real confidence | Coverage without mutation testing can be theater |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09-01 | Repudiation | 401 handling | mitigate | 401 surfaces a rotate-key `<app-error-state>` with a Go-to-settings path — never a console-only error (QUAL-07, ASVS V7) |
| T-05-09-02 | Repudiation | block-action failures | mitigate | approve/discard/edit failures surface via `<app-error-state>` (folded IN-01) — the user is told the action didn't go through (QUAL-09) |
| T-05-09-03 | Tampering | type-tightening | mitigate | Zero `any` in Phase 5 production paths (web-search transport casts narrowed); documented third-party exceptions only (QUAL-01, ASVS V5) |
| T-05-09-04 | Repudiation | coverage confidence | mitigate | Stryker mutation floor pinned on validators.ts at the measured baseline — coverage proven to kill mutants, not just execute lines (QUAL-10) |
| T-05-09-05 | Tampering | a11y regressions | mitigate | axe-core color-contrast enforced across 8 pages + manual keyboard/contrast checkpoint (QUAL-08) |
</threat_model>

<verification>
- chat-page spec passes (401 + block-action surfacing)
- `npx stryker run` meets the pinned floor; `ng build --configuration=production` exits 0
- Full Karma suite green, zero serious/critical axe (incl. color-contrast) across 8 pages
- Manual keyboard + contrast checkpoint approved
</verification>

<success_criteria>
The 401 rotate-key prompt, block-action error surfacing, `any`-free production paths, the pinned mutation floor, and the full a11y pass (automated + manual) close the quality sweep — QUAL-01/07/08/09/10 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-search-grounding-quality-sweep/05-09-SUMMARY.md`
</output>

---
phase: 01-foundations
plan: 06
type: execute
wave: 2
depends_on:
  - 02
files_modified:
  - src/app/services/cardio.service.ts
  - src/app/services/weight.service.ts
  - src/app/services/readings.service.ts
  - src/app/services/diet.service.ts
  - src/app/services/chat.service.ts
  - src/app/features/diet/diet-page.component.ts
  - src/app/features/charts/charts-page.component.ts
  - src/app/features/reports/report-page.component.ts
autonomous: true
requirements:
  - FOUND-02
tags:
  - shared
  - utilities
  - uuid
  - chart-grouping
  - retrofit
must_haves:
  truths:
    - "Every entity in `cardio/weight/readings/diet/chat` services gets its `id` from `generateId()` (`src/app/shared/id.ts`); the 5 duplicated `generateUUID` helpers are deleted (per FOUND-02, D-02 area, CONCERNS.md)"
    - "`diet-page.component.ts` no longer contains a private `generateUUID` (per CONCERNS.md 'ID generation leaked into a feature component'); ID assignment happens inside `DietService.addMeal` / `addSavedFood` paths"
    - "`charts-page.component.ts` and `report-page.component.ts` import `groupByDay`/`toDateKey`/`round2` from `src/app/shared/chart-grouping`; the private functions in `charts-page.component.ts:594-646` are deleted; both pages share the same averaging math (per CONCERNS.md 'Same-day reading averaging is duplicated, not shared')"
    - "No `Math.random()` UUID generator remains in `src/app/services/**` or `src/app/features/**` (verifiable via grep gate)"
  artifacts:
    - path: "src/app/services/cardio.service.ts"
      provides: "Imports generateId from '../shared/id'; local generateUUID deleted"
    - path: "src/app/services/weight.service.ts"
      provides: "Same retrofit"
    - path: "src/app/services/readings.service.ts"
      provides: "Same retrofit"
    - path: "src/app/services/diet.service.ts"
      provides: "Same retrofit; absorbs the id-generation that was leaking into diet-page"
    - path: "src/app/services/chat.service.ts"
      provides: "Same retrofit"
    - path: "src/app/features/diet/diet-page.component.ts"
      provides: "Local generateUUID deleted; meal/food shapes passed to DietService without `id`"
    - path: "src/app/features/charts/charts-page.component.ts"
      provides: "Imports from '../../shared/chart-grouping'; private toDateKey/groupByDay/round2 deleted (lines 594-646)"
    - path: "src/app/features/reports/report-page.component.ts"
      provides: "Imports from '../../shared/chart-grouping'; same averaging math as charts-page"
  key_links:
    - from: "src/app/services/cardio.service.ts addSession"
      to: "src/app/shared/id.ts generateId"
      via: "import { generateId } from '../shared/id'"
      pattern: "generateId\\(\\)"
    - from: "src/app/features/charts/charts-page.component.ts"
      to: "src/app/shared/chart-grouping.ts"
      via: "import { groupByDay, toDateKey, round2 } from '../../shared/chart-grouping'"
      pattern: "from '\\.\\./\\.\\./shared/chart-grouping'"
    - from: "src/app/features/reports/report-page.component.ts"
      to: "src/app/shared/chart-grouping.ts"
      via: "same import"
      pattern: "from '\\.\\./\\.\\./shared/chart-grouping'"
---

<objective>
Wave 2 retrofit of the shared utilities created in plan 02. Two parallel concerns landed in one plan because the consumers don't conflict file-by-file:

1. **id.ts adoption** (replaces 6 duplicated `generateUUID` copies — 5 services + 1 feature component):
   - 5 service files: `cardio.service.ts`, `weight.service.ts`, `readings.service.ts`, `diet.service.ts`, `chat.service.ts` — each replaces the local `generateUUID` with `import { generateId } from '../shared/id'`
   - 1 feature component: `diet-page.component.ts` — DELETE the local `generateUUID` outright; lift id-assignment into `DietService` per CONCERNS.md "ID generation leaked into a feature component"

2. **chart-grouping.ts adoption** (replaces drifting averaging math):
   - `charts-page.component.ts` — DELETE the private `toDateKey`/`groupByDay`/`round2` at lines 594-646; import the shared versions
   - `report-page.component.ts` — refactor to import from the same place; replace whatever local averaging logic exists at `:472-565` (per CONCERNS.md, this page may not currently share averaging — D-02 explicitly fixes that drift)

Both concerns are FOUND-02 retrofit work and depend on plan 02 having created the shared modules. They share NO files with each other (services vs charts/reports), so they belong in one plan to keep the plan count manageable.

**This plan does NOT touch empty/error retrofit (plan 07) or subscription hygiene (plan 07). Those are page-template and component-class changes that touch the same files. They land in plan 07 sequentially after this plan to avoid same-wave file conflicts.**

Purpose: FOUND-02 — Shared utilities consolidated; 6 duplicates collapsed; chart-vs-report drift eliminated.

Output: 8 files modified — all imports rewired; 6 duplicate functions deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@./.planning/REQUIREMENTS.md
@./.planning/phases/01-foundations/01-CONTEXT.md
@./.planning/phases/01-foundations/01-RESEARCH.md
@./.planning/phases/01-foundations/01-PATTERNS.md
@./.planning/codebase/CONCERNS.md
@./.planning/codebase/CONVENTIONS.md
@./src/app/shared/id.ts
@./src/app/shared/chart-grouping.ts
@./src/app/services/cardio.service.ts
@./src/app/services/weight.service.ts
@./src/app/services/readings.service.ts
@./src/app/services/diet.service.ts
@./src/app/services/chat.service.ts
@./src/app/features/diet/diet-page.component.ts
@./src/app/features/charts/charts-page.component.ts
@./src/app/features/reports/report-page.component.ts

<interfaces>
src/app/shared/id.ts (created in plan 02):
```typescript
export function generateId(): string;  // primary: crypto.randomUUID; fallback: Math.random
```

src/app/shared/chart-grouping.ts (created in plan 02):
```typescript
export function toDateKey(isoString: string): string;
export function groupByDay<T extends { date: string }>(
  readings: T[],
  extractor: (r: T) => number[],
): { labels: string[]; averages: number[][] };
export function round2(value: number): number;
```

Existing duplicated `generateUUID` shape in 6 sites (delete each):
```typescript
function generateUUID(): string {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, (c) => {
    const r = Math.random() * 16 | 0;
    const v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
}
```

Locations from PATTERNS §"Pattern S-1" lines 700-723:
- src/app/services/cardio.service.ts:24-30
- src/app/services/weight.service.ts:24
- src/app/services/readings.service.ts:38
- src/app/services/diet.service.ts:26
- src/app/services/chat.service.ts:13-19
- src/app/features/diet/diet-page.component.ts:9-15 (DELETE; id-generation moves into DietService; call sites at :774, :846)

charts-page.component.ts:594-646 holds `toDateKey`, `groupByDay`, `round2` as file-private functions. After this plan they're deleted (replaced by shared imports).

report-page.component.ts:472-565 currently does its own filtering and may not share averaging — refactor to import from shared/chart-grouping (per CONCERNS.md fragile area).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace generateUUID in 5 services with generateId import</name>
  <files>src/app/services/cardio.service.ts, src/app/services/weight.service.ts, src/app/services/readings.service.ts, src/app/services/diet.service.ts, src/app/services/chat.service.ts</files>
  <read_first>
    - ./src/app/services/cardio.service.ts (lines 1-30: imports + generateUUID definition + class start. Find every CALL site of generateUUID — typically inside addSession/addEntry/etc.)
    - ./src/app/services/weight.service.ts (same pattern; line ~24)
    - ./src/app/services/readings.service.ts (line ~38; multiple call sites for blood-pressure / glucose / ketone add methods)
    - ./src/app/services/diet.service.ts (line ~26; CALL sites at lines 74, 80, 120, 163, 250, 267, 403 per RESEARCH §line 1180)
    - ./src/app/services/chat.service.ts (lines 13-19; multiple call sites for conversation / message creation)
    - ./src/app/shared/id.ts (the new exporter — must exist before this plan starts; it does, per plan 02)
    - ./.planning/codebase/CONCERNS.md "Duplicated, non-cryptographic UUID generator" (lines 16-21)
    - ./.planning/codebase/CONVENTIONS.md §"Imports" (relative paths only; group: angular > third-party > local services > local models)
  </read_first>
  <action>
    For EACH of the 5 service files, perform exactly these edits (preserve all other code):

    1. **Add import**: At the top of the file, in the local-services import group (after rxjs imports, before `../models/*`):
       ```typescript
       import { generateId } from '../shared/id';
       ```
       Single quotes, named import, kebab-case path.

    2. **Delete the local generateUUID function**: Locate the `function generateUUID(): string { ... }` block (typically just above the `@Injectable` decorator). Delete it entirely including its JSDoc comment. The shape to delete in all 5 files matches:
       ```typescript
       /**
        * Generates a UUID v4 string.
        */
       function generateUUID(): string {
         return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, (c) => {
           const r = Math.random() * 16 | 0;
           const v = c === 'x' ? r : (r & 0x3 | 0x8);
           return v.toString(16);
         });
       }
       ```

    3. **Replace every call**: Find every `generateUUID()` call in the file and replace with `generateId()` (same call shape, different name).

    Per-file expected call counts (use these as sanity checks; the exact count may shift if the codebase has been modified, but should be close):
    - cardio.service.ts: 1-2 calls (typically inside `addSession`)
    - weight.service.ts: 1-2 calls (inside `addEntry`)
    - readings.service.ts: 3 calls (inside `addBloodPressure`, `addBloodGlucose`, `addKetone`)
    - diet.service.ts: ~7 calls per RESEARCH §line 1180 (lines 74, 80, 120, 163, 250, 267, 403 — verify by grep)
    - chat.service.ts: 2-3 calls (conversation creation + message creation)

    Acceptance: each file has ZERO occurrences of `generateUUID` (function name) and at least ONE occurrence of `generateId(` (call site). The import statement is present.

    Do NOT change validation, observable patterns, sorting, ISO timestamp generation, or any other logic. The diff should be import-line additions, function-block deletions, and rename-only call-site changes.
  </action>
  <verify>
    <automated>for f in src/app/services/cardio.service.ts src/app/services/weight.service.ts src/app/services/readings.service.ts src/app/services/diet.service.ts src/app/services/chat.service.ts; do grep -q "import { generateId } from '../shared/id'" "$f" || (echo "MISSING import in $f" &amp;&amp; exit 1); grep -q "generateUUID" "$f" &amp;&amp; (echo "STILL HAS generateUUID in $f" &amp;&amp; exit 1) || true; grep -q "generateId(" "$f" || (echo "NO generateId() call in $f" &amp;&amp; exit 1); done; ng test --no-watch --browsers=ChromeHeadless --include='src/app/services/cardio.service.spec.ts' --include='src/app/services/weight.service.spec.ts' --include='src/app/services/readings.service.spec.ts' --include='src/app/services/diet.service.spec.ts' --include='src/app/services/chat.service.spec.ts'</automated>
  </verify>
  <acceptance_criteria>
    - All 5 service files import `generateId` from `'../shared/id'`: `for f in src/app/services/{cardio,weight,readings,diet,chat}.service.ts; do grep -q "import { generateId } from '../shared/id'" "$f" || exit 1; done`
    - All 5 service files have ZERO `generateUUID` references: `! grep -nE 'generateUUID' src/app/services/{cardio,weight,readings,diet,chat}.service.ts`
    - All 5 service files have at least one `generateId()` call: `for f in src/app/services/{cardio,weight,readings,diet,chat}.service.ts; do grep -q "generateId(" "$f" || exit 1; done`
    - All 5 existing service specs still pass: `ng test --no-watch --browsers=ChromeHeadless --include='src/app/services/cardio.service.spec.ts' --include='src/app/services/weight.service.spec.ts' --include='src/app/services/readings.service.spec.ts' --include='src/app/services/diet.service.spec.ts' --include='src/app/services/chat.service.spec.ts'` exits 0
    - No service file calls `Math.random()` for ID generation (defensive grep gate, excluding comments and string literals): `! grep -nE 'Math\\.random\\(\\)' src/app/services/{cardio,weight,readings,diet,chat}.service.ts | grep -v '^[[:space:]]*\\*\\|^[[:space:]]*//' || true`
    - `ng build --configuration=production` exits 0 (strict TS still passes)
  </acceptance_criteria>
  <done>5 services use `generateId()` from shared module; all local `generateUUID` copies deleted; existing service specs still green; production build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Delete diet-page.component.ts local generateUUID; lift id assignment into DietService</name>
  <files>src/app/features/diet/diet-page.component.ts, src/app/services/diet.service.ts</files>
  <read_first>
    - ./src/app/features/diet/diet-page.component.ts:1-30 (the leaked `generateUUID` at lines 9-15 per CONCERNS.md) and lines 770-850 (the call sites at :774 and :846 — adding meal items / saved foods)
    - ./src/app/services/diet.service.ts (verify which methods accept input shapes — `addMeal`, `addSavedFood`, etc. — and where IDs are currently assigned)
    - ./.planning/codebase/CONCERNS.md "ID generation leaked into a feature component" (lines 23-28)
    - ./CLAUDE.md "Component Pattern" (components handle UI only; delegate business logic to services)
  </read_first>
  <action>
    Two coordinated edits to fix the architectural leak:

    1. **In `diet-page.component.ts`**:
       - Delete the local `generateUUID` function at lines 9-15 (its JSDoc included).
       - At every CALL site (typically lines 774 and 846 — verify via grep before editing), STOP creating IDs in the component. Instead, build the input shape WITHOUT `id` and let `DietService` assign IDs internally.
       - Example transformation (the exact shape depends on the existing call):
         - BEFORE: `this.dietService.addMeal({ id: generateUUID(), date, items: [...] })`
         - AFTER: `this.dietService.addMeal({ date, items: [...] })`
       - The component should now have ZERO id-generation logic and ZERO `generateUUID` references.

    2. **In `diet.service.ts`**:
       - Confirm `addMeal` (and any related method called from the component) currently expects an `id` field on its input. If so, switch to a `Create*` input type (omitting `id`) — `DietService` is already responsible for assigning `id`/`createdAt`/`updatedAt` for cardio/weight/readings services per CONVENTIONS.md "Pair the stored entity with a 'create' interface".
       - Concretely: if the input type is `Meal`, change the parameter to `Omit<Meal, 'id' | 'createdAt' | 'updatedAt'>` (or use the existing `Create*` interface if it exists in the diet model). Inside the service, generate id via `generateId()` (already imported by Task 1 in this plan).
       - If `Create*` interfaces don't exist for diet entities, define them in `src/app/models/diet.model.ts` mirroring the `CreateCardioSession` pattern at `src/app/models/cardio-session.model.ts:49`. This is a small, additive type-only change.

    Important: This is the only place in Phase 1 where business logic shifts between layers. Make sure existing diet service specs still pass — if input-shape changes break specs, update the specs to match the new contract (the contract change is the WHOLE POINT — keep the test fixtures aligned). If a spec change is non-trivial, prefer adding an explicit overload over changing the existing signature; document any decisions in the SUMMARY.

    Conventions: 2-space indent, single quotes, named imports, ISO 8601 timestamps via `new Date().toISOString()` (existing pattern in cardio.service.ts:74), no `null` for absent fields per CLAUDE.md.
  </action>
  <verify>
    <automated>! grep -nE 'generateUUID' src/app/features/diet/diet-page.component.ts &amp;&amp; ! grep -nE 'Math\\.random\\(\\)' src/app/features/diet/diet-page.component.ts &amp;&amp; ng test --no-watch --browsers=ChromeHeadless --include='src/app/services/diet.service.spec.ts' &amp;&amp; ng build --configuration=production 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `diet-page.component.ts` has ZERO `generateUUID` references: `! grep -nE 'generateUUID' src/app/features/diet/diet-page.component.ts`
    - `diet-page.component.ts` does not import `generateId` (id-generation belongs in DietService): `! grep -nE 'generateId' src/app/features/diet/diet-page.component.ts`
    - `diet-page.component.ts` does not call `Math.random()` for id generation: `! grep -nE 'Math\\.random\\(\\)' src/app/features/diet/diet-page.component.ts`
    - `diet.service.ts` still imports `generateId` (from Task 1): `grep -q "import { generateId } from '../shared/id'" src/app/services/diet.service.ts`
    - Existing diet service spec still passes (or has been updated to match the contract change): `ng test --no-watch --browsers=ChromeHeadless --include='src/app/services/diet.service.spec.ts'` exits 0
    - Production build succeeds: `ng build --configuration=production` exits 0
  </acceptance_criteria>
  <done>diet-page no longer generates IDs; DietService assigns IDs at the service layer; CLAUDE.md "Component Pattern" violation eliminated.</done>
</task>

<task type="auto">
  <name>Task 3: Wire charts-page + report-page to shared chart-grouping; delete in-place duplicate</name>
  <files>src/app/features/charts/charts-page.component.ts, src/app/features/reports/report-page.component.ts</files>
  <read_first>
    - ./src/app/features/charts/charts-page.component.ts:594-646 (the three private functions to delete; existing JSDoc at :594-597 and :606-609 documents timezone-locality decision — preserve in shared/chart-grouping.ts JSDoc, drop here)
    - ./src/app/features/reports/report-page.component.ts:472-565 (the date-range filtering / averaging logic to refactor to shared imports)
    - ./src/app/shared/chart-grouping.ts (the shared module created in plan 02 — already exports `toDateKey`, `groupByDay`, `round2`)
    - ./src/app/features/charts/charts-page.component.ts (full file scan: every reference to `toDateKey`, `groupByDay`, `round2` from outside the bottom block. After this plan they all resolve to the imported version.)
    - ./.planning/codebase/CONCERNS.md "Same-day reading averaging is duplicated, not shared" (lines 74-79)
    - ./.planning/codebase/CONVENTIONS.md §"Imports" order
  </read_first>
  <action>
    Two file edits with the same pattern (both pages import from shared):

    1. **In `charts-page.component.ts`**:
       - Add at the top of the file in the local-imports group (after Angular + third-party imports, before relative model/service imports):
         ```typescript
         import { groupByDay, toDateKey, round2 } from '../../shared/chart-grouping';
         ```
       - DELETE the three function definitions at lines 594-646 (the entire block: `function toDateKey(...)`, `function groupByDay(...)`, `function round2(...)`, including any JSDoc above them).
       - Existing call sites in charts-page that referenced these functions now resolve to the imported versions — no call-site changes needed (same names, same signatures, same module-level visibility for the consumer).

    2. **In `report-page.component.ts`**:
       - Add the same import at the top:
         ```typescript
         import { groupByDay, toDateKey, round2 } from '../../shared/chart-grouping';
         ```
       - Examine `:472-565` carefully. This block currently does its own filtering and may have its own (drifting) averaging math per CONCERNS.md. Refactor to use the imported `groupByDay`/`toDateKey`/`round2` for any same-day grouping/averaging path. If a custom path doesn't EXIST in this range (per CONCERNS.md uncertainty), at minimum:
         - If the report page calls `toDateKey` or similar local helpers, replace with the imports.
         - If the report page calls `Math.round(value * 100) / 100`, replace with `round2(value)`.
         - If the report page builds its own day-bucket Map<string, ...>, replace with `groupByDay(...)`.

       The acceptance criterion is that report-page **shares the same averaging math** as charts-page. The exact diff depends on what's currently in :472-565. Document in the SUMMARY which lines were refactored and which were left alone (e.g., date-range filtering that doesn't involve grouping stays as-is; that's `date-range.ts`'s concern, not `chart-grouping.ts`'s).

    Constraints:
    - Do NOT modify `date-range.ts` (CONCERNS.md "Date math uses UTC offsets" is a Phase 2 / DIET-08 concern; out of scope).
    - Do NOT change `getStorageInfo()` or any storage-related code (Phase 5 / QUAL-02).
    - Do NOT change templates, styles, or anything UI-rendering-related (those are plan 07's scope).

    Conventions: 2-space indent, single quotes, named imports, relative paths only.
  </action>
  <verify>
    <automated>grep -q "import { groupByDay, toDateKey, round2 } from '../../shared/chart-grouping'" src/app/features/charts/charts-page.component.ts &amp;&amp; ! grep -nE '^[[:space:]]*function (toDateKey|groupByDay|round2)' src/app/features/charts/charts-page.component.ts &amp;&amp; grep -q "import { groupByDay, toDateKey, round2 } from '../../shared/chart-grouping'" src/app/features/reports/report-page.component.ts &amp;&amp; ng build --configuration=production 2>&amp;1 | tail -10 &amp;&amp; ng test --no-watch --browsers=ChromeHeadless --include='src/app/shared/chart-grouping.spec.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `charts-page.component.ts` imports from shared/chart-grouping: `grep -q "import { groupByDay, toDateKey, round2 } from '../../shared/chart-grouping'" src/app/features/charts/charts-page.component.ts`
    - The three private functions are GONE from charts-page.component.ts: `! grep -nE '^[[:space:]]*function (toDateKey|groupByDay|round2)\\b' src/app/features/charts/charts-page.component.ts`
    - `report-page.component.ts` imports from shared/chart-grouping: `grep -q "import { groupByDay, toDateKey, round2 } from '../../shared/chart-grouping'" src/app/features/reports/report-page.component.ts`
    - The shared chart-grouping spec still passes (the helpers themselves are unchanged): `ng test --no-watch --browsers=ChromeHeadless --include='src/app/shared/chart-grouping.spec.ts'` exits 0
    - Production build succeeds: `ng build --configuration=production` exits 0
    - `date-range.ts` is unchanged (Phase 2 fixes timezone issue): `git diff --name-only src/app/shared/date-range.ts | wc -l` equals 0
  </acceptance_criteria>
  <done>charts-page and report-page both consume the shared chart-grouping module; private duplicates deleted from charts-page; report-page now shares the same averaging math (CONCERNS.md drift fixed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service `addX` methods → caller (component or other service) | After this plan, all id-generation happens at the service layer; components no longer mint IDs (CLAUDE.md "Component Pattern") |
| `chart-grouping.ts` ↔ both consumer pages | Single source of truth for day-grouping/averaging — eliminates the drift CONCERNS.md flagged as the most likely regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Math.random IDs collide | mitigate | This plan replaces 6 `Math.random()`-based UUID generators with `crypto.randomUUID()` (with documented fallback). Acceptance grep gate: zero `generateUUID` and zero `Math.random()` in the 8 modified files. |
| T-06-02 | Repudiation | Charts vs reports drift in same-day averaging (b6149d2 regression) | mitigate | Single shared module forces both pages through the same code path. plan 02's spec covers the regression case for value 10+20 → average 15; this plan ensures both consumers go through that code. |
| T-06-03 | Information Disclosure | Component-side id generation makes audit difficult | mitigate | DietService is now the single source of truth for diet-entity IDs. Acceptance criterion: no `generateUUID` and no `generateId` references in `diet-page.component.ts` (id-gen belongs in service layer). |
</threat_model>

<verification>
- All 5 service `*.service.spec.ts` still pass under `ng test --no-watch --browsers=ChromeHeadless`
- Diet service spec adapted (if input contract changed) and still passes
- `ng build --configuration=production` exits 0
- Zero `generateUUID` or `Math.random()` UUID references in services/features tree (production paths)
- charts-page private functions removed from `:594-646`
- report-page imports same chart-grouping module
</verification>

<success_criteria>
- 8 files modified: 5 services + 1 feature component (diet-page) + 2 chart consumer pages (charts-page, report-page)
- 6 duplicated `generateUUID` functions DELETED (5 services + 1 in diet-page)
- 3 chart-helper functions DELETED from charts-page.component.ts
- 1 service (DietService) now owns id-generation for diet entities
- 2 pages (charts, reports) share the same `groupByDay`/`toDateKey`/`round2` source
- All existing service specs still pass; production build still succeeds
- No new dependencies; no new files (this plan is pure consumer-side wiring)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations/01-06-SUMMARY.md` per the standard summary template.
</output>

---
phase: 04-agentic-loop-citation-ui
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/services/anthropic-api.service.ts
  - src/app/services/anthropic-api.service.spec.ts
  - src/app/services/fitness-context.service.ts
  - src/app/services/fitness-context.service.spec.ts
autonomous: true
requirements: [CHAT-10]
user_setup: []

must_haves:
  truths:
    - "AnthropicApiService.sendMessage accepts full MessageCreateParams including tools[] and cache_control (the Phase 3 Omit is removed)"
    - "AnthropicApiService.countTokens accepts system: string | TextBlockParam[] and tools so the cache_control'd prefix + tools block are counted"
    - "FitnessContextService.buildSystemPrompt emits a slim ~500-token key-facts header — NOT the full dataset snapshot"
    - "Two consecutive buildSystemPrompt() builds produce a byte-identical cacheable prefix (fixed field order, deterministic 'today' slot, stable empty-profile omission)"
    - "The stable system-prefix block carries cache_control: { type: 'ephemeral' }"
  artifacts:
    - path: "src/app/services/anthropic-api.service.ts"
      provides: "Widened transport: tools[] + cache_control on send; system+tools on countTokens"
      contains: "MessageCreateParams"
    - path: "src/app/services/fitness-context.service.ts"
      provides: "Slim ~500-tok cacheable header + cache_control breakpoint; keeps CHAT-11 delimiter wrap"
      contains: "ephemeral"
    - path: "src/app/services/fitness-context.service.spec.ts"
      provides: "E7 cache-prefix byte-stability + header-budget spec"
      contains: "byte-identical"
  key_links:
    - from: "src/app/services/fitness-context.service.ts"
      to: "anthropic cache_control"
      via: "cache_control: { type: 'ephemeral' } on the stable prefix TextBlockParam"
      pattern: "cache_control.*ephemeral"
---

<objective>
Widen the SDK transport (`anthropic-api.service.ts`) to carry `tools[]` + `cache_control` on send and `system`/`tools` on `countTokens`, and slim `FitnessContextService.buildSystemPrompt` to a byte-stable ~500-token cacheable key-facts header with a `cache_control: ephemeral` breakpoint (CHAT-10 / D-15).

Purpose: This is the CHAT-10 / E7 surface. The Phase 3 transport forbade `tools` at the type level (the loop needs them lifted). The current `FitnessContextService` stuffs the full dataset and embeds `new Date().toISOString()` mid-prompt (fitness-context.service.ts:43-44) — both break a stable cacheable prefix (Pitfall 4). Slimming it (the full data now arrives via `query_*` tools) + a stable prefix turns repeat-turn system cost from 1× to 0.1×. These two files are the transport+context foundation the Wave 2 agentic loop builds on.

Output: widened `anthropic-api.service.ts` + spec; slimmed `fitness-context.service.ts` + spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agentic-loop-citation-ui/04-CONTEXT.md
@.planning/phases/04-agentic-loop-citation-ui/04-AI-SPEC.md
@.planning/phases/04-agentic-loop-citation-ui/04-PATTERNS.md

<interfaces>
<!-- Verified from the codebase + installed @anthropic-ai/sdk@0.92.0. -->

Current transport (src/app/services/anthropic-api.service.ts):
```typescript
// :63-66 — Phase 3 forbids tools at the type level:
sendMessage(apiKey: string, params: Omit<MessageCreateParams, 'tools' | 'tool_choice'>): Observable<Message>
// :80-83 — countTokens narrow system:
countTokens(apiKey: string, params: { model: string; system?: string; messages: MessageParam[] }): Observable<number>
// :95-110 getClient cache (dangerouslyAllowBrowser: true); :112-128 mapError; AnthropicApiError.statusCode (chat-page depends on === 401)
```
This is the SOLE @anthropic-ai/sdk importer (D-17 chokepoint) — keep all SDK types here.

Verified SDK shapes (04-RESEARCH SDK surfaces table): `MessageCountTokensParams` accepts `system: string | TextBlockParam[]` and `tools?: MessageCountTokensTool[]`; `cache_control?: CacheControlEphemeral` present on `TextBlockParam`.

Current context assembly (src/app/services/fitness-context.service.ts):
```typescript
// :18 buildSystemPrompt(): Observable<string>
// :43 sections.push(`## Current Fitness Data (as of ${new Date().toISOString()})`)  ← VOLATILE mid-prompt
// :44 buildFitnessDataSnapshot(data, toolSettings)  ← FULL dataset (lines 76-243)
// :53-58 wrapUntrusted(tag, content)  ← CHAT-11 delimiter guard (KEEP)
// :60-65 hasAnyProfileContent / :69-72 profile sections via wrapUntrusted  ← KEEP, stable empty-profile omission
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen anthropic-api.service transport for tools[] + cache_control + countTokens</name>
  <files>src/app/services/anthropic-api.service.ts, src/app/services/anthropic-api.service.spec.ts</files>
  <read_first>
    - src/app/services/anthropic-api.service.ts (sendMessage 63-73, countTokens 80-93, getClient 95-110, mapError 112-128, AnthropicApiError 20)
    - src/app/services/anthropic-api.service.spec.ts (existing spec to extend)
    - .planning/phases/04-agentic-loop-citation-ui/04-PATTERNS.md "anthropic-api.service.ts (MODIFY)" (lines 172-191)
    - .planning/phases/04-agentic-loop-citation-ui/04-RESEARCH.md "Widen the transport surface for Phase 4" (lines 368-380)
    - .planning/phases/04-agentic-loop-citation-ui/04-AI-SPEC.md §3 Core Imports (lines 144-168)
  </read_first>
  <action>
    Modify `src/app/services/anthropic-api.service.ts` (this stays the SOLE @anthropic-ai/sdk importer — D-17):
    - `sendMessage`: remove the `Omit<MessageCreateParams, 'tools' | 'tool_choice'>` — accept full `MessageCreateParams` (now with `tools[]` + `cache_control`). Update the JSDoc at 60-62 to note the Phase 3 "tools forbidden" rule is intentionally lifted in Phase 4. Body (the `from(promise.catch(mapError))` Promise→Observable bridge) is UNCHANGED.
    - `countTokens`: widen the params type to `{ model: string; system?: string | TextBlockParam[]; messages: MessageParam[]; tools?: MessageCountTokensTool[] }`. Import `TextBlockParam` and `MessageCountTokensTool` from `@anthropic-ai/sdk/resources/messages`. Pass `tools` through to `.countTokens(...)`. Body otherwise unchanged.
    - Preserve VERBATIM: `getClient` cache + `dangerouslyAllowBrowser: true`, `mapError`, `friendlyMessage`, and `AnthropicApiError.statusCode` (chat-page.component.ts depends on `=== 401`).

    Extend `anthropic-api.service.spec.ts`: add a case asserting `sendMessage` forwards a `tools` array to the mocked client `messages.create`, and a `countTokens` case asserting a `TextBlockParam[]` system + `tools` are forwarded to `messages.countTokens`. Keep existing 401/error-mapping specs green.
  </action>
  <verify>
    <automated>ng test --no-watch --browsers=ChromeHeadless --include='**/anthropic-api.service.spec.ts'</automated>
  </verify>
  <acceptance_criteria>
    - anthropic-api.service.spec.ts runs green (exit 0)
    - `grep -q "Omit<MessageCreateParams" src/app/services/anthropic-api.service.ts` FAILS (the Omit is removed)
    - `grep -q "tools" src/app/services/anthropic-api.service.ts` succeeds (countTokens + sendMessage forward tools)
    - `grep -q "TextBlockParam" src/app/services/anthropic-api.service.ts` succeeds (widened countTokens system type)
    - `grep -q "dangerouslyAllowBrowser" src/app/services/anthropic-api.service.ts` still succeeds (preserved)
    - Spec asserts a `tools` array reaches the mocked `messages.create`
  </acceptance_criteria>
  <done>Transport carries tools[] + cache_control on send and system+tools on countTokens; 401 path + SDK chokepoint preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Slim FitnessContextService to a byte-stable ~500-token cacheable prefix</name>
  <files>src/app/services/fitness-context.service.ts, src/app/services/fitness-context.service.spec.ts</files>
  <read_first>
    - src/app/services/fitness-context.service.ts (buildSystemPrompt 18-49, the volatile timestamp :43, buildFitnessDataSnapshot 76-243, wrapUntrusted 53-58, hasAnyProfileContent 60-65, profile sections 69-72)
    - src/app/services/fitness-context.service.spec.ts (existing spec to extend)
    - .planning/phases/04-agentic-loop-citation-ui/04-PATTERNS.md "fitness-context.service.ts (MODIFY)" (lines 214-224)
    - .planning/phases/04-agentic-loop-citation-ui/04-RESEARCH.md Pitfall 4 (lines 266-270) + Pattern 4 (lines 208-211)
    - .planning/phases/04-agentic-loop-citation-ui/04-AI-SPEC.md §3 Context Window Strategy (lines 408-410) + §4b Prompt Engineering Discipline (lines 479-483 — what the cached prefix carries)
  </read_first>
  <action>
    Modify `src/app/services/fitness-context.service.ts` to emit a slim, byte-stable cacheable prefix (D-15 / Pitfall 4):
    - Change the return type of `buildSystemPrompt()` from `Observable<string>` to `Observable<TextBlockParam[]>` (import `TextBlockParam` from `@anthropic-ai/sdk/resources/messages` — fitness-context is NOT in the D-17 chokepoint, but TextBlockParam is a param-only type; if a lint rule forbids the SDK import here, instead define a minimal local structural type `{ type: 'text'; text: string; cache_control?: { type: 'ephemeral' } }` and let the loop/transport accept it. CHECK the SDK-import lint rule first — RESEARCH says only anthropic-api.service.ts + chat-block-serializer.ts may import the SDK, so PREFER the local structural type here to stay inside the chokepoint).
    - Build ONE stable prefix text block carrying `cache_control: { type: 'ephemeral' }`, containing in FIXED field order: a slim key-facts header — units (lbs / km / mmol/L), profile-if-present (via the existing `wrapUntrusted` + `hasAnyProfileContent` stable-omission path, KEEP 53-72), high-level counts / latest values only (e.g. "Weight entries: 38, latest 182 lbs"; "Readings: 120 BP / 42 glucose / …") — NOT the full snapshot, plus the stable grading/coaching instructions.
    - Move the volatile "today" value (currently `new Date().toISOString()` at :43) into a DETERMINISTIC slot — a single trailing date line in a fixed position, or a separate non-cached trailing block — so the cacheable prefix hash does not churn. Do NOT embed it mid-prefix.
    - REMOVE the full-dataset stuffing: delete/retire `buildFitnessDataSnapshot`'s inclusion in the prompt (76-243) — the per-domain detail now arrives via the Plan 02 `query_*` tools. Keep the `wrapUntrusted` CHAT-11 delimiter wrapping for any user-authored profile text that remains in the header (prompt-injection guard — do NOT drop it).
    - Keep the redaction toggles behavior (the D-09 redaction toggles referenced in PATTERNS) consistent.

    This return-type change ripples to chat.service (Plan 04 consumes `buildSystemPrompt()`) and the widened transport `system` param (Task 1) — Plan 04 handles the consumer side; this plan only changes the producer.

    Extend `fitness-context.service.spec.ts` (E7):
    - Assert two consecutive `buildSystemPrompt()` builds (with the SAME mock data, profile present, and profile absent) produce a byte-identical cacheable prefix block (serialize the cache_control'd block to a string and compare).
    - Assert the cacheable prefix carries `cache_control: { type: 'ephemeral' }`.
    - Assert the header is slim (length proxy: the cacheable prefix text ≤ a fixed budget, e.g. ≤ 2500 chars as a ~500-token proxy) and does NOT contain a full per-entry dump (e.g. assert it does not contain N>cap individual entry lines).
    - Assert the empty-profile omission is stable (profile-absent build produces a prefix that simply omits the profile section, same field order otherwise).
  </action>
  <verify>
    <automated>ng test --no-watch --browsers=ChromeHeadless --include='**/fitness-context.service.spec.ts'</automated>
  </verify>
  <acceptance_criteria>
    - fitness-context.service.spec.ts runs green (exit 0)
    - `grep -q "ephemeral" src/app/services/fitness-context.service.ts` succeeds (cache_control breakpoint)
    - `grep -q "new Date().toISOString()" src/app/services/fitness-context.service.ts` — the volatile timestamp is NO LONGER embedded mid-prefix (either removed or relocated to a deterministic trailing slot; assert the cacheable prefix is byte-identical across two builds via the spec)
    - Spec contains a "byte-identical" / "byte-stable" cacheable-prefix assertion across two builds (E7)
    - `grep -q "wrapUntrusted" src/app/services/fitness-context.service.ts` still succeeds (CHAT-11 delimiter guard preserved)
    - The full per-entry snapshot is no longer stuffed into the system prompt (verify the spec asserts a bounded header, not a full dump)
    - `ng build --configuration=production` exits 0
  </acceptance_criteria>
  <done>Slim ~500-token header with a byte-stable cacheable cache_control prefix; full data moved to query_* tools; CHAT-11 delimiter wrap kept.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-authored profile/notes → system prompt | Untrusted user strings enter the cached system prefix |
| app → api.anthropic.com | The sole external egress (SDK transport) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering / Elevation (prompt injection) | fitness-context.service buildSystemPrompt | mitigate | The CHAT-11 `wrapUntrusted` `<user_*>` delimiter wrapping is preserved for any profile text remaining in the slim header; user-authored content cannot escape its delimiter into instructions. Grep gate asserts wrapUntrusted still present. |
| T-04-03-02 | Denial of Service (cost) | cache prefix stability | mitigate | Byte-stable cacheable prefix (fixed field order, deterministic "today" slot, stable empty-profile omission) → `cache_control: ephemeral` yields 0.1× reads instead of 1.25× writes. E7 byte-identical spec proves stability (Pitfall 4). |
| T-04-03-03 | Information Disclosure | transport sendMessage | accept | Only outbound traffic is the user-initiated request to `api.anthropic.com` via the SDK with `dangerouslyAllowBrowser` (single-user, own key — accepted in Phase 3). No change to key handling this phase. |
</threat_model>

<verification>
- `ng test --no-watch --browsers=ChromeHeadless` full suite green (no regression)
- `ng build --configuration=production` exits 0
- D-17 chokepoint intact: `@anthropic-ai/sdk` imported only in anthropic-api.service.ts (+ chat-block-serializer.ts); fitness-context uses a local structural type if the lint rule forbids the SDK import
</verification>

<success_criteria>
- Transport accepts tools[] + cache_control; countTokens accepts system TextBlockParam[] + tools
- Slim byte-stable cacheable prefix with cache_control: ephemeral (E7 proven by spec)
- Full dataset removed from the prompt; CHAT-11 delimiter guard kept
</success_criteria>

<output>
After completion, create `.planning/phases/04-agentic-loop-citation-ui/04-03-SUMMARY.md`
</output>

---
phase: 04-agentic-loop-citation-ui
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/app/features/chat/chat-message-list.component.ts
  - src/app/features/chat/chat-message-list.component.spec.ts
autonomous: true
requirements: [CHAT-06, CHAT-07, CHAT-08, CHAT-09]
user_setup: []

must_haves:
  truths:
    - "An assistant message whose prose contains 'Smith et al. 2019', a bare DOI, and a bare URL renders ZERO <a href> anchors (citation-link guard, E1)"
    - "Only a structured TextCitation (search_result_location / web_search_result_location) may become an <a> — proven by a synthetic forward-compat assertion"
    - "Each confidence badge carries color + icon + text label (triple-encoding); a from-research·speculative claim is visually distinct from a from-your-data·strong claim"
    - "tool_use query_* blocks render as collapsed <details> disclosures (one per call, execution order); tool_result renders the real expandable viewer"
    - "A from-research claim carries the 'general knowledge — not a live source' qualifier and zero links; an unbadged claim renders as plain prose with no chip"
  artifacts:
    - path: "src/app/features/chat/chat-message-list.component.ts"
      provides: "Extended @switch: ClaimSpan badges + citation guard + tool-call disclosures"
      contains: "parseClaimSpans"
    - path: "src/app/features/chat/chat-message-list.component.spec.ts"
      provides: "E1 adversarial citation-guard + E4 triple-encoding badge specs"
      contains: "querySelectorAll('a')"
  key_links:
    - from: "src/app/features/chat/chat-message-list.component.ts"
      to: "src/app/services/confidence-attribution-parser.ts"
      via: "parseClaimSpans(block.text) in the 'text' @case"
      pattern: "parseClaimSpans"
---

<objective>
Extend the `chat-message-list` `@switch (block.type)` render to: parse each `text` block into `ClaimSpan[]` and render inline confidence + source badges (CHAT-07/08, D-07/D-08/D-10), render `query_*` `tool_use`/`tool_result` blocks as collapsed `<details>` disclosures (CHAT-06, D-05/D-06), and enforce the citation-link guard (CHAT-09, D-13) — the headline E1 adversarial surface where free-text author-year/DOI/URL must render as plain inert text.

Purpose: This is the user-facing transparency + epistemic-honesty surface and the highest-stakes guard (E1). Per UI-SPEC (APPROVED) it EXTENDS the Phase 3 `@switch`, does not rebuild it, preserves the `<div class="message-content">` wrapper (Phase 1 characterization contract), and ships only inline templates + component-scoped styles (no new global CSS). The citation guard ships and is adversarially tested NOW, one phase before web citations exist (D-13).

Output: extended `chat-message-list.component.ts` + spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agentic-loop-citation-ui/04-CONTEXT.md
@.planning/phases/04-agentic-loop-citation-ui/04-UI-SPEC.md
@.planning/phases/04-agentic-loop-citation-ui/04-PATTERNS.md

<interfaces>
<!-- Verified from the codebase. EXTEND the existing @switch; preserve the message-content wrapper. -->

Existing render (src/app/features/chat/chat-message-list.component.ts):
```
:30  <div class="message-content">@for (block of msg.blocks; track $index) {
:31    @switch (block.type) {
:32      @case ('text')        {<span class="block-text">{{ block.text }}</span>}   // EXTEND: ClaimSpan[] + badges + guard
:33-39  @case ('tool_use')    { <app-pending-pill ... (approve)/(discard)/(edit) /> } // KEEP for WRITE proposals; ADD query_* disclosure
:41-42  @case ('tool_result') { <div class="tool-result-placeholder">...</div> }     // REPLACE with real <details> viewer
:106 .message-content { ... }   :111-120 .tool-result-placeholder voice (reuse)   :148-152 @keyframes blink (reuse for in-flight pulse)
:184-189 scrollToBottom()
```

From Plan 01: `import { parseClaimSpans } from '../../services/confidence-attribution-parser';` + `ClaimSpan`/`Confidence`/`Attribution` types from `../../models/ai-chat.model`.

LOCKED visual contract (04-UI-SPEC — APPROVED 6/6):
- Confidence tiers: calm `strong`/`moderate` → bg #eef6ec, text #2e7d32, icon `✓` (strong) / `≈` (moderate); alert `weak`/`animal-only`/`anecdotal`/`speculative` → bg #fdf3e7, text #b9770e, icon `⚠`.
- Source chip: `your data` 📈 / `research` 📚 (neutral #f0f0f0 bg, #2c3e50 text). from-research ALSO renders literal qualifier `general knowledge — not a live source` (D-12) + zero links.
- Badge: 14px/600, 4px 8px padding, 4px radius, inline-flex, 4px glyph↔label gap, order [confidence][source][optional view-source link]; row wraps (flex-wrap) on narrow viewports.
- Tool disclosure: native <details>/<summary>, #f8f9fa bg, 1px solid #ddd, 4px radius, min-height 44px summary (touch target), collapsed default, one per call in execution order. Expanded body: `Tool: {name}` / `Query:` params / `Result:` in <pre> white-space:pre-wrap. In-flight (no result yet): present-tense "…" summary + @keyframes blink pulse, non-expandable.
- Per-tool summary verbs LOCKED (04-UI-SPEC Copywriting): query_weight_entries → in-flight `⚖️ Reading your weight entries…` / resolved `⚖️ Read {n} weight entries · {range}`, etc. {n}/{range} computed by the RENDERER from tool input/result, NOT model prose; omit the `· {…}` clause if unavailable.
- a11y: confidence span aria-label="Confidence: {grade}"; source aria-label="Source: from your data" / "Source: from research — general knowledge, not a live source"; in-flight row role="status" aria-live="polite"; <summary> accessible name "Show what the AI looked at: {summary}"; glyphs are real text content (NOT CSS pseudo-elements). expectNoSeriousA11yViolations with disableRules:['color-contrast'].
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Citation-link guard + confidence/source badges in the 'text' branch (E1/E4)</name>
  <files>src/app/features/chat/chat-message-list.component.ts, src/app/features/chat/chat-message-list.component.spec.ts</files>
  <read_first>
    - src/app/features/chat/chat-message-list.component.ts (the @switch 30-44, message-content wrapper :30, styles 106-152)
    - src/app/features/chat/chat-message-list.component.spec.ts (existing spec + expectNoSeriousA11yViolations usage)
    - src/app/services/confidence-attribution-parser.ts (parseClaimSpans from Plan 01)
    - src/app/shared/a11y-test-helpers.ts (expectNoSeriousA11yViolations + disableRules option)
    - .planning/phases/04-agentic-loop-citation-ui/04-UI-SPEC.md Color §Confidence/Source badge palette (lines 114-138), Copywriting §badges (lines 184-196), Accessibility Contract (lines 283-313)
    - .planning/phases/04-agentic-loop-citation-ui/04-AI-SPEC.md §3 citation note (lines 170, 323) + Pitfall 6 (lines 338), §5 E1 (line 517)
    - .planning/phases/04-agentic-loop-citation-ui/04-RESEARCH.md "citation-link guard" Code Example (lines 330-340), Pitfall 1 (lines 248-252), Validation §2 (lines 460-464)
  </read_first>
  <behavior>
    - RED first (E1 — the headline test). In chat-message-list.component.spec.ts:
      - Render an assistant message whose `text` block prose contains `Smith et al. 2019`, a bare DOI `10.1000/xyz`, and a bare URL `https://pubmed.ncbi.nlm.nih.gov/12345` (NO structured citations) → assert `fixture.nativeElement.querySelectorAll('a').length === 0` for model-authored prose.
      - Forward-compat: render a synthetic TextBlock carrying a `search_result_location` citation → assert the guard PERMITS an `<a>` for it (proves the guard distinguishes, not suppress-all). Keep green for Phase 5. (If ChatBlock has no citation field this phase per 04-RESEARCH Open Q #3, assert this at the guard-function level instead: `isLinkableCitation({type:'search_result_location'}) === true` and the prose path produces no anchor.)
      - Assert NO `bypassSecurityTrust*` call exists in the render path (DomSanitizer spy or source grep).
      - E4 badges: render claims with [evidence: strong][source: data] and [evidence: speculative][source: research] → assert each badge has a visible icon glyph as real text content (✓ / ⚠ present in textContent), an aria-label ("Confidence: strong" / "Confidence: speculative"; "Source: from your data" / "Source: from research — general knowledge, not a live source"), and the two render with distinct tier classes (calm vs alert).
      - from-research claim renders the literal `general knowledge — not a live source` qualifier and zero <a>.
      - Unbadged claim (no token) renders plain prose, no chip.
      - expectNoSeriousA11yViolations(fixture.nativeElement, { disableRules: ['color-contrast'] }).
  </behavior>
  <action>
    Extend the `'text'` `@case` in `chat-message-list.component.ts`. Preserve the `<div class="message-content">` wrapper VERBATIM (Phase 1 characterization spec asserts on it). Replace `<span class="block-text">{{ block.text }}</span>` with a render of `parseClaimSpans(block.text)` (call it in a component method that memoizes per block — do NOT call in a template binding that re-runs every change detection; compute once when blocks change). For each `ClaimSpan`:
    - Render `span.text` as PLAIN TEXT (Angular interpolation `{{ }}` — which auto-escapes; NEVER `[innerHTML]`, NEVER `DomSanitizer.bypassSecurityTrust*`, NEVER markdown autolinking). This is the citation guard: model prose can never become an `<a>` (D-13). Add a guard helper `isLinkableCitation(c): boolean { return c.type === 'search_result_location' || c.type === 'web_search_result_location'; }` (copy 04-RESEARCH Code Example) used ONLY for structured `TextCitation` objects — and since no tool produces them in Phase 4, no `<a>` ever renders from prose.
    - If `span.confidence` is set: render an inline `<span>` confidence chip — class by tier (calm: strong/moderate; alert: weak/animal-only/anecdotal/speculative), icon glyph (`✓` strong, `≈` moderate, `⚠` all alert) as real text content, text label = the verbatim taxonomy word, `aria-label="Confidence: {grade}"`. Colors per the LOCKED palette (#eef6ec/#2e7d32 calm, #fdf3e7/#b9770e alert) in component-scoped `styles`.
    - If `span.source` is set: render an inline source `<span>` chip — `📈 your data` (aria-label "Source: from your data") or `📚 research` (aria-label "Source: from research — general knowledge, not a live source"); neutral #f0f0f0/#2c3e50. For `research`, ALSO render the literal adjacent text `general knowledge — not a live source` (D-12), zero hyperlinks.
    - If neither set → plain prose, no chip (D-09 safe degradation).
    - Badge row order [confidence][source]; `flex-wrap: wrap`; no `bypassSecurityTrust*` anywhere.

    Add component-scoped `styles` for the chips (no new global CSS — UI-SPEC). GREEN: implement until spec passes.
  </action>
  <verify>
    <automated>ng test --no-watch --browsers=ChromeHeadless --include='**/chat-message-list.component.spec.ts'</automated>
  </verify>
  <acceptance_criteria>
    - chat-message-list.component.spec.ts runs green (exit 0)
    - Spec contains the E1 adversarial assertion `querySelectorAll('a').length` === 0 on author-year/DOI/URL prose
    - `grep -q "bypassSecurityTrust" src/app/features/chat/chat-message-list.component.ts` FAILS (forbidden)
    - `grep -q "innerHTML" src/app/features/chat/chat-message-list.component.ts` FAILS (no raw HTML injection of model prose)
    - `grep -q "parseClaimSpans" src/app/features/chat/chat-message-list.component.ts` succeeds
    - `grep -q "message-content" src/app/features/chat/chat-message-list.component.ts` still succeeds (wrapper preserved)
    - Spec asserts triple-encoding (icon glyph in textContent + aria-label + tier class) for a calm and an alert badge
    - Spec asserts the `general knowledge — not a live source` qualifier on a from-research claim
    - expectNoSeriousA11yViolations passes with disableRules:['color-contrast']
  </acceptance_criteria>
  <done>Citation guard proven by E1 adversarial spec (zero anchors from prose); triple-encoded confidence + source badges render with the LOCKED palette; safe-degradation plain prose for unbadged claims.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tool_use/tool_result collapsed <details> disclosures + in-flight progress (E12)</name>
  <files>src/app/features/chat/chat-message-list.component.ts, src/app/features/chat/chat-message-list.component.spec.ts</files>
  <read_first>
    - src/app/features/chat/chat-message-list.component.ts (tool_use @case 33-39, tool_result @case 41-42, .tool-result-placeholder 111-120, @keyframes blink 148-152)
    - .planning/phases/04-agentic-loop-citation-ui/04-UI-SPEC.md Component Inventory §Tool-call disclosure (lines 244, 251-256), Copywriting §Live tool-call progress (lines 160-182 — the LOCKED per-tool summary verbs), Layout §in-stream order (lines 273-279)
    - .planning/phases/04-agentic-loop-citation-ui/04-CONTEXT.md D-05/D-06 (lines 38-40), D-01 (line 32)
  </read_first>
  <behavior>
    - In chat-message-list.component.spec.ts:
      - A `query_*` `tool_use` block renders as a `<details>` with a `<summary>` (collapsed by default — `<details>` without `open`), min-height 44px, accessible name "Show what the AI looked at: …".
      - A `tool_result` block renders the real expandable viewer: `Tool: {name}` / `Query:` / `Result:` in a `<pre>` (replaces the Phase 3 static `.tool-result-placeholder`).
      - The collapsed summary line for a resolved query_weight_entries uses the LOCKED verb `⚖️ Read {n} weight entries · {range}` with {n}/{range} computed by the renderer from the block (NOT model prose) — and OMITS the `· {…}` clause when count/range is unavailable.
      - An in-flight tool row (tool_use with no paired result yet) renders the present-tense `…` summary with role="status" aria-live="polite" and is non-expandable.
      - A WRITE-proposal tool_use (the memory pending pill) still renders `<app-pending-pill>` (KEEP — only query_* get the disclosure).
      - expectNoSeriousA11yViolations(..., { disableRules: ['color-contrast'] }).
  </behavior>
  <action>
    Extend the `'tool_use'` and `'tool_result'` `@case` branches:
    - `'tool_use'`: branch on whether it is a write proposal (memory — keep `<app-pending-pill>`) vs a `query_*` call (render the collapsed disclosure). Use a renderer helper to derive the humanized summary from the tool name + input/result via the LOCKED per-tool verb table (04-UI-SPEC Copywriting lines 171-180): present-tense while in-flight (no result), past-tense `Read {n} … · {range}` when resolved. Compute `{n}`/`{range}` from the block data, never from model prose; omit the clause if unavailable. In-flight row: `role="status"` `aria-live="polite"`, the existing `@keyframes blink` pulse, non-expandable.
    - `'tool_result'`: replace the static `.tool-result-placeholder` with a native `<details>` (collapsed default) whose `<summary>` (min-height 44px, accessible name "Show what the AI looked at: {summary}") shows the humanized line, and whose expanded body shows `Tool: {name}` (14px/600), `Query:` params + `Result:` formatted output in `<pre>` `white-space: pre-wrap` (reuse the existing `.tool-result-placeholder` voice for the box). One disclosure per tool call, in execution order (the existing `@for` order preserves this — D-06).
    - Component-scoped styles only; reuse #f8f9fa surface, 1px solid #ddd, 4px radius, the blink keyframes. No new global CSS.

    GREEN: implement until spec passes.
  </action>
  <verify>
    <automated>ng test --no-watch --browsers=ChromeHeadless --include='**/chat-message-list.component.spec.ts'</automated>
  </verify>
  <acceptance_criteria>
    - chat-message-list.component.spec.ts runs green (exit 0)
    - `grep -q "<details" src/app/features/chat/chat-message-list.component.ts` succeeds (native disclosure)
    - Spec asserts a collapsed `<details>` (not open by default) for a query_* tool block
    - Spec asserts the in-flight row carries role="status" aria-live="polite"
    - Spec asserts the resolved summary uses a LOCKED verb (e.g. textContent contains "Read" + "weight entries") computed from the block, and OMITS the `·` clause when count is absent
    - `<app-pending-pill>` still rendered for memory write-proposal tool_use (grep + spec)
    - expectNoSeriousA11yViolations passes with disableRules:['color-contrast']
    - `ng build --configuration=production` exits 0
  </acceptance_criteria>
  <done>query_* tool calls render as collapsed <details> disclosures with LOCKED humanized summaries (renderer-derived, not model prose), in-flight progress is announced via aria-live, and write proposals still use the pending pill.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model-authored prose → DOM render | Untrusted model text (possibly citation-shaped or HTML-shaped) crosses into the rendered DOM |
| tool_result content → disclosure | Tool output rendered to the user as an audit trail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Spoofing (fabricated clickable citation) — HIGHEST STAKES | text @case render | mitigate | Model prose rendered via auto-escaping `{{ }}` interpolation ONLY; NO `[innerHTML]`, NO `bypassSecurityTrust*`, NO markdown autolinking. The sole path to `<a>` is a structured `TextCitation` (`isLinkableCitation`), and none exist in Phase 4 → zero anchors. E1 adversarial spec asserts `querySelectorAll('a').length === 0`. Grep gates forbid bypassSecurityTrust/innerHTML. |
| T-04-05-02 | Spoofing (XSS via model HTML) | text @case render | mitigate | Same interpolation-only rule prevents model-authored markup from executing. No `bypassSecurityTrust*`. |
| T-04-05-03 | Information Disclosure (transparency theater) | tool disclosure summary | mitigate | The humanized `{n}`/`{range}` summary is computed by the RENDERER from the actual tool block, never free-authored by the model (E12); the clause is omitted rather than guessed when data is unavailable. Spec asserts renderer-derived summary. |
| T-04-05-04 | Tampering (low-confidence read as strong) | confidence/source badge | mitigate | Triple-encoding (color + icon + text) so a skimming user distinguishes from-research·speculative from from-your-data·strong; color is never the only channel (CLAUDE.md a11y). Spec asserts all three channels (E4). |
</threat_model>

<verification>
- `ng test --no-watch --browsers=ChromeHeadless` full suite green (Phase 1 chat-page characterization spec still passes — message-content wrapper preserved)
- `ng build --configuration=production` exits 0
- Grep gates: zero `bypassSecurityTrust`, zero `innerHTML` in chat-message-list.component.ts
</verification>

<success_criteria>
- E1 citation guard: zero anchors from model prose (adversarial spec)
- E4 triple-encoded confidence + source badges, color never alone
- query_* tool disclosures collapsed by default, renderer-derived summaries (E12)
</success_criteria>

<output>
After completion, create `.planning/phases/04-agentic-loop-citation-ui/04-05-SUMMARY.md`
</output>

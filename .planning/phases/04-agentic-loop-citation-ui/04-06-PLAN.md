---
phase: 04-agentic-loop-citation-ui
plan: 06
type: execute
wave: 3
depends_on: [04, 05]
files_modified:
  - src/app/features/chat/chat-page.component.ts
  - src/app/features/chat/chat-page.component.spec.ts
  - src/app/features/chat/pending-pill.component.ts
autonomous: true
requirements: [CHAT-02, CHAT-05, CHAT-06]
user_setup: []

must_haves:
  truths:
    - "Sending a chat message drives ChatService.runAgenticLoop and renders incremental ChatTurnEvents (in-flight tool rows → resolved summaries → done) into the message list"
    - "The loop subscription pipes through takeUntilDestroyed(this.destroyRef) — navigating away mid-loop stops the loop (and billing)"
    - "A real write proposal surfaced mid-loop renders as a pending pill; approve/discard/edit route through the existing onBlockAction handler"
    - "The dev-only seed button / synthetic-pill seeding is removed or gated so it cannot fire alongside real proposals"
    - "A turn_limit event renders the honest 'Reached the tool-use limit' notice; a transport failure surfaces <app-error-state> with Retry (401 path preserved)"
    - "The Phase 1 chat-page characterization spec still passes (no regression to DOM/flow)"
  artifacts:
    - path: "src/app/features/chat/chat-page.component.ts"
      provides: "Drives runAgenticLoop events into the view; gates dev-seed; preserves 401 handling"
      contains: "runAgenticLoop"
    - path: "src/app/features/chat/chat-page.component.spec.ts"
      provides: "Loop-orchestration spec: multi-emit events, turn_limit notice, error-state, takeUntilDestroyed"
      contains: "runAgenticLoop"
  key_links:
    - from: "src/app/features/chat/chat-page.component.ts"
      to: "src/app/services/chat.service.ts runAgenticLoop"
      via: ".pipe(takeUntilDestroyed(this.destroyRef)).subscribe(event => ...)"
      pattern: "runAgenticLoop.*takeUntilDestroyed"
---

<objective>
Rewire `chat-page.component.ts` to drive `ChatService.runAgenticLoop` (the Wave 2 loop) and render its incremental `ChatTurnEvent`s into the extended message list (Plan 05): live in-flight tool rows → resolved summaries → final answer (D-01), the turn-limit notice (D-04) and terminal notices (D-16), with `takeUntilDestroyed` as the cancellation seam. Remove/gate the Phase 3 dev-only seed button so real write proposals (D-03) fire cleanly, and give the pending pill a real `resolvedAt`.

Purpose: This is the loop's UI orchestration — it closes CHAT-05/CHAT-06 at the page level and is where success criterion #1 ("ask how's my weight trending → AI fetches real data via query_* and answers") becomes user-visible. It must preserve the Phase 1 chat-page characterization spec (the regression net) and the existing 401 error path.

Output: rewired `chat-page.component.ts` + spec; `pending-pill.component.ts` real `resolvedAt`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agentic-loop-citation-ui/04-CONTEXT.md
@.planning/phases/04-agentic-loop-citation-ui/04-UI-SPEC.md
@.planning/phases/04-agentic-loop-citation-ui/04-PATTERNS.md

<interfaces>
<!-- Verified from the codebase. -->

chat-page.component.ts touchpoints:
```
:4   import { takeUntilDestroyed } from '@angular/core/rxjs-interop'
:191 destroyRef = inject(DestroyRef)
:209-216 ngOnInit subscribe chain → initializeActiveConversationAndConsumeSeed()
:234-272 initializeActiveConversationAndConsumeSeed (has a dev-SEED branch — GATE/REMOVE)
:280-318 appendSeededPill (dev-only synthetic pill — GATE/REMOVE)
:330-335 consumeDevSeedIfPresent (dev-only — GATE/REMOVE)
:343+   onBlockAction(event) → routes pill approve/discard/edit to chat.service (KEEP — now receives REAL proposals)
:483-515 sendMessage subscribe (the Phase 3 single-shot path — REWIRE to runAgenticLoop)
:500    if (err instanceof AnthropicApiError && err.statusCode === 401) (KEEP the 401 mapping)
```
StorageService.consumeDevSeed() is the dev-seed source the gated methods read.

From Plan 04: `chatService.runAgenticLoop(conversationId, apiKey): Observable<ChatTurnEvent>` (multi-emit). From Plan 01: `ChatTurnEvent` union.

LOCKED copy (04-UI-SPEC Copywriting): turn-limit notice `Reached the tool-use limit ({n} turns) — answering with the data gathered so far.` (role="status" aria-live="polite"); max_tokens `This answer was cut off at the length limit. Ask me to continue if you'd like the rest.`; refusal `I'm not able to answer that one.`; transport error via <app-error-state>: title `The AI request didn't go through`, message `Something interrupted the request. Check your connection and API key, then try again.`, Retry. Focus stays on chat input during the loop (do NOT yank focus per ChatTurnEvent — 04-UI-SPEC Focus management lines 311-313); the one intentional focus move is the pending-pill primary action when a proposal surfaces (existing Phase 3 behavior).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drive runAgenticLoop events into the view + terminal/turn-limit notices</name>
  <files>src/app/features/chat/chat-page.component.ts, src/app/features/chat/chat-page.component.spec.ts</files>
  <read_first>
    - src/app/features/chat/chat-page.component.ts (sendMessage subscribe 483-515, onBlockAction 343+, destroyRef 191, 401 path 500)
    - src/app/features/chat/chat-page.component.spec.ts (the Phase 1 characterization spec — MUST keep passing)
    - src/app/services/chat.service.ts (runAgenticLoop signature from Plan 04)
    - .planning/phases/04-agentic-loop-citation-ui/04-PATTERNS.md "chat-page.component.ts (MODIFY)" (lines 249-258)
    - .planning/phases/04-agentic-loop-citation-ui/04-UI-SPEC.md Copywriting §turn-limit/terminal/transport (lines 198-222), Focal Point (lines 146-153), Focus management (lines 311-313)
    - .planning/phases/04-agentic-loop-citation-ui/04-CONTEXT.md D-01/D-04 (lines 32, 35)
  </read_first>
  <action>
    Rewire the send flow in `chat-page.component.ts` from the single-shot `chatService.sendMessage` (483-515) to `chatService.runAgenticLoop(this.activeConversationId, apiKey)`:
    - `.pipe(takeUntilDestroyed(this.destroyRef)).subscribe({ next: event => this.handleTurnEvent(event), error: err => this.handleLoopError(err) })`. The subscription is MULTI-EMIT (not single completion) — the `next` handler renders incremental `ChatTurnEvent`s.
    - Add `handleTurnEvent(event: ChatTurnEvent)`: on `tool_use_started`/`tool_result`/`assistant_text` → refresh the active conversation from storage so the extended message-list (Plan 05) re-renders the new blocks (in-flight → resolved → prose); on `turn_limit` → set a view flag/notice with the LOCKED copy `Reached the tool-use limit ({n} turns) — answering with the data gathered so far.` rendered as `role="status"` `aria-live="polite"`; on `done` with `stopReason 'max_tokens'` → the LOCKED truncation notice, `'refusal'` → the LOCKED refusal message rendered as a plain assistant message, `'end_turn'`/`'stop_sequence'` → no notice.
    - Add `handleLoopError(err)`: preserve the existing `err instanceof AnthropicApiError && err.statusCode === 401` mapping (do NOT regress it); surface `<app-error-state>` with the LOCKED transport-failure copy + Retry (Retry re-invokes the loop for the last user message). Phase 5 owns the 401-specific rotate-key UX — Phase 4 surfaces a generic recoverable error, must not crash or log-only.
    - Focus: do NOT move focus on each event (keyboard-trap risk — 04-UI-SPEC); focus stays on the chat input. The pending-pill primary-action focus move on a surfaced proposal is the existing Phase 3 behavior (unchanged).
    - Keep `onBlockAction` (343+) — it now receives REAL proposals; route approve→`chatService.approveToolUseBlock`, discard/edit unchanged.

    Extend `chat-page.component.spec.ts` (additive — do not break the Phase 1 characterization specs):
    - Mock `chatService.runAgenticLoop` to return a Subject/of-sequence of ChatTurnEvents; assert sending a message subscribes to it and that successive events refresh the rendered conversation.
    - Assert a `turn_limit` event renders the LOCKED notice with role="status".
    - Assert a `done` `max_tokens` renders the truncation notice; `refusal` renders the refusal message.
    - Assert the loop subscription is piped through `takeUntilDestroyed` (drive a teardown and assert no further emissions are processed) — the cancellation seam.
    - Assert a thrown `AnthropicApiError(statusCode:401)` still routes to the 401 path (existing behavior preserved) and a generic error surfaces `<app-error-state>` with Retry.
  </action>
  <verify>
    <automated>ng test --no-watch --browsers=ChromeHeadless --include='**/chat-page.component.spec.ts'</automated>
  </verify>
  <acceptance_criteria>
    - chat-page.component.spec.ts runs green (exit 0) INCLUDING the pre-existing Phase 1 characterization specs (no regression)
    - `grep -q "runAgenticLoop" src/app/features/chat/chat-page.component.ts` succeeds
    - `grep -q "runAgenticLoop" src/app/features/chat/chat-page.component.ts && grep -q "takeUntilDestroyed" src/app/features/chat/chat-page.component.ts` (loop subscription piped through cancellation seam)
    - `grep -q "statusCode === 401" src/app/features/chat/chat-page.component.ts` still succeeds (401 path preserved)
    - Spec asserts a turn_limit notice (role="status") and the max_tokens/refusal terminal notices with the LOCKED copy
    - Spec asserts takeUntilDestroyed cancellation (teardown stops further event processing)
  </acceptance_criteria>
  <done>The page drives the multi-emit loop into the view with live progress + turn-limit/terminal notices, preserves the 401 path and the Phase 1 characterization spec, and cancels on teardown.</done>
</task>

<task type="auto">
  <name>Task 2: Gate/remove the dev-only seed button + real pending-pill resolvedAt</name>
  <files>src/app/features/chat/chat-page.component.ts, src/app/features/chat/pending-pill.component.ts, src/app/features/chat/chat-page.component.spec.ts</files>
  <read_first>
    - src/app/features/chat/chat-page.component.ts (initializeActiveConversationAndConsumeSeed 234-272, appendSeededPill 280-318, consumeDevSeedIfPresent 330-335 — the dev-seed machinery)
    - src/app/features/chat/pending-pill.component.ts (the resolvedTime() / resolvedAt handling — Phase 3 stubbed new Date())
    - .planning/phases/04-agentic-loop-citation-ui/04-PATTERNS.md "chat-page.component.ts" dev-seed removal (line 255), 04-CONTEXT.md D-12 reference (line 114)
    - .planning/phases/04-agentic-loop-citation-ui/04-UI-SPEC.md Component Inventory §pending-pill (line 248)
  </read_first>
  <action>
    Remove or gate the Phase 3 dev-only seed machinery so it cannot fire synthetic pending pills alongside REAL loop proposals (04-CONTEXT D-12 / RESEARCH "Reusable Assets"):
    - In `chat-page.component.ts`: remove the dev-SEED branch from `initializeActiveConversationAndConsumeSeed` (234-272) and the `appendSeededPill` (280-318) + `consumeDevSeedIfPresent` (330-335) methods — OR gate them behind a build-time/dev-only flag that is OFF in production. PREFER removal if the StorageService `consumeDevSeed` source has no remaining non-dev consumer; if removal would break the Phase 1/Phase 3 empty-state characterization assertion, gate instead and document why in the SUMMARY. After this, the most-recent-conversation auto-select on init MUST still work (do not regress the ngOnInit ordering fix from plan 03-06) — keep the conversation-load + auto-select, drop only the seed-consume/append.
    - In `pending-pill.component.ts`: wire `resolvedTime()` to read the real `resolvedAt` from the resolved block instead of the Phase 3 `new Date()` stub (a data change, not a visual change — 04-UI-SPEC line 248). If `resolvedAt` is absent on legacy blocks, fall back gracefully (omit the timestamp rather than fabricate).

    Update `chat-page.component.spec.ts`: remove/adjust any spec that drove the dev-seed path; assert the seed button/synthetic-pill path no longer fires real-time (or is gated off). Keep the auto-select-most-recent-conversation behavior asserted (regression guard for plan 03-06).
  </action>
  <verify>
    <automated>ng test --no-watch --browsers=ChromeHeadless --include='**/chat-page.component.spec.ts' --include='**/pending-pill.component.spec.ts'</automated>
  </verify>
  <acceptance_criteria>
    - chat-page + pending-pill specs run green (exit 0)
    - The dev-seed path is removed or gated: `grep -n "appendSeededPill\|consumeDevSeed" src/app/features/chat/chat-page.component.ts` shows EITHER no matches (removed) OR all matches inside a dev-only gate (document which in SUMMARY)
    - `grep -q "resolvedAt" src/app/features/chat/pending-pill.component.ts` succeeds (real resolved timestamp)
    - Auto-select-most-recent-conversation on init still asserted in the spec (plan 03-06 not regressed)
    - `ng build --configuration=production` exits 0
  </acceptance_criteria>
  <done>Dev-seed synthetic-pill path removed/gated so real proposals fire cleanly; pending pill shows a real resolvedAt; ngOnInit auto-select preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loop ChatTurnEvent stream → view | Multi-emit events drive incremental render; teardown must stop the loop |
| model write proposal → pending pill → user approval | Mutation gate at the UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Denial of Service (cost / runaway loop on navigate-away) | runAgenticLoop subscription | mitigate | The loop subscription pipes through `takeUntilDestroyed(this.destroyRef)`; navigating away tears down → `cancelled` (Plan 04) → the loop stops calling the API (stops billing). Spec asserts teardown stops event processing. |
| T-04-06-02 | Elevation of Privilege (unapproved write persisted) | onBlockAction / pending pill | mitigate | Real write proposals surface as pending pills; persistence happens ONLY on explicit approve (confirm-before-write D-03). Discard/edit do not persist a write. No second dialog — the pill IS the confirmation. |
| T-04-06-03 | Tampering (dev-seed firing alongside real proposals) | dev-seed machinery | mitigate | The Phase 3 dev-only seed path is removed or gated OFF in production so it cannot inject synthetic pills that could be mistaken for / collide with real loop proposals (D-12). |
| T-04-06-04 | Information Disclosure (silent failure) | handleLoopError | mitigate | A transport/401 failure surfaces a visible `<app-error-state>` with Retry (never console-only / silent); the existing 401 mapping is preserved. |
</threat_model>

<verification>
- `ng test --no-watch --browsers=ChromeHeadless` FULL suite green — Phase 1 chat-page characterization spec + Phase 3 specs all still pass
- `ng build --configuration=production` exits 0
- Manual (operator, post-merge): run `ng serve`, ask "how's my weight trending vs my goal?" → confirm live tool rows fire, the AI answers from real logs, badges + disclosures render (CHAT-02/05/06 manual dims per 04-VALIDATION Manual-Only)
</verification>

<success_criteria>
- runAgenticLoop drives live multi-emit progress into the view with turn-limit/terminal notices
- takeUntilDestroyed cancels the loop on navigate-away; 401 path preserved
- Dev-seed gated/removed; real proposals render as pending pills with real resolvedAt
- Phase 1 characterization spec still green
</success_criteria>

<output>
After completion, create `.planning/phases/04-agentic-loop-citation-ui/04-06-SUMMARY.md`
</output>

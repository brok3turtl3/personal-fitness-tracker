---
phase: 04-agentic-loop-citation-ui
plan: 04
type: tdd
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - src/app/services/chat.service.ts
  - src/app/services/chat.service.spec.ts
autonomous: true
requirements: [CHAT-02, CHAT-05]
user_setup: []

must_haves:
  truths:
    - "ChatService.runAgenticLoop runs while(stop_reason === 'tool_use'), bounded by maxAgentTurns, emitting ChatTurnEvent"
    - "Every StopReason branch is handled: end_turn/stop_sequence/max_tokens/refusal complete; pause_turn resumes without counting the turn; tool_use continues; default completes"
    - "query_* tool calls auto-execute via ToolRegistryService.dispatch and append their tool_result in a USER turn"
    - "A mid-loop write proposal surfaces a pending pill and is fed a synthetic 'not yet persisted' tool_result — the loop NEVER blocks"
    - "Hitting maxAgentTurns emits turn_limit and makes one final create WITHOUT tools for a best-effort answer"
    - "Unsubscribing sets cancelled → the loop stops (no further mock API calls); estimateTokens (len/4) is gone from the window decision"
  artifacts:
    - path: "src/app/services/chat.service.ts"
      provides: "runAgenticLoop(): Observable<ChatTurnEvent> — the bounded agentic loop"
      contains: "runAgenticLoop"
    - path: "src/app/services/chat.service.spec.ts"
      provides: "E2 spec — every stop_reason + cap + write-proposal-no-block + user-turn tool_result"
      contains: "stop_reason"
  key_links:
    - from: "src/app/services/chat.service.ts"
      to: "src/app/services/tool-registry.service.ts"
      via: "isWriteProposal + dispatch"
      pattern: "toolRegistry\\.(dispatch|isWriteProposal|definitions)"
    - from: "src/app/services/chat.service.ts"
      to: "src/app/services/chat.service.ts buildApiMessages"
      via: "reuse the Phase 3 tool_result→user-turn serializer machinery"
      pattern: "buildApiMessages"
---

<objective>
Add `runAgenticLoop()` to `ChatService` — the bounded `while (stop_reason === 'tool_use')` multi-emit Observable loop (CHAT-05) that dispatches `query_*` tools through `ToolRegistryService` (CHAT-02), handles every terminal `StopReason` (D-16), defers write proposals without blocking (D-03), degrades gracefully at the `maxAgentTurns` cap (D-04), and retires the `len/4` token heuristic for the window decision (D-15).

Purpose: This is the E2 critical-failure surface — the loop must NEVER wedge. Every `StopReason` branch handled, the cap degrades visibly, write proposals never freeze the loop, and `tool_result` blocks always serialize into a `user` turn (the commit `49e275b` regression). It reuses the Phase 3 `buildApiMessages` wire machinery verbatim — do NOT re-solve tool_result placement.

Output: `chat.service.ts` with `runAgenticLoop` + `surfacePendingProposal`; extended `chat.service.spec.ts` driving a mocked transport through every branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agentic-loop-citation-ui/04-CONTEXT.md
@.planning/phases/04-agentic-loop-citation-ui/04-AI-SPEC.md
@.planning/phases/04-agentic-loop-citation-ui/04-PATTERNS.md

<interfaces>
<!-- Verified from the codebase. The loop REUSES chat.service's own primitives. -->

Existing chat.service.ts primitives to reuse UNCHANGED:
```
buildApiMessages(conversation): MessageParam[]   // :370-436 — already splits tool_result into a USER turn
                                                  //   (:406-417, commit 49e275b) + coalesces same-role turns (:423-433)
approveToolUseBlock(...)                          // :265-325 — atomic persist of tool_use + paired tool_result in ONE saveData
estimateTokens(text): number                      // :26-28 — Math.ceil(len/4); RETIRE from the window decision
constructor deps                                  // :34-39 — StorageService, AnthropicApiService, AISettingsService, FitnessContextService
sendMessage(...)                                  // :98-187 — single-shot Phase 3 path; the loop is a NEW method alongside it
```

From Plan 01 (ai-chat.model.ts): `ChatTurnEvent` union, `Confidence`/`Attribution`/`ClaimSpan`.
From Plan 02 (tool-registry.service.ts): `dispatch(name, input): Promise<string>`, `definitions(): ToolDefinition[]`, `isWriteProposal(name): boolean`.
From Plan 03: `AnthropicApiService.sendMessage` now accepts `tools[]`; `buildSystemPrompt(): Observable<TextBlockParam[]-or-local-structural>`; `countTokens` accepts system+tools.

Canonical loop body — copy VERBATIM from 04-AI-SPEC §3 Entry Point Pattern (lines 194-309) / 04-RESEARCH Code Examples (lines 284-328). Verified against installed SDK StopReason union: 'end_turn'|'max_tokens'|'stop_sequence'|'tool_use'|'pause_turn'|'refusal'.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: TDD the agentic loop stop-reason switch + cap + write-proposal-no-block</name>
  <files>src/app/services/chat.service.ts, src/app/services/chat.service.spec.ts</files>
  <read_first>
    - src/app/services/chat.service.ts (sendMessage 98-187, buildApiMessages 370-436, approveToolUseBlock 265-325, estimateTokens 26-28, constructor 34-39)
    - src/app/services/chat.service.spec.ts (existing spec to extend — the mocked AnthropicApiService pattern)
    - src/app/services/tool-registry.service.ts (dispatch, definitions, isWriteProposal from Plan 02)
    - src/app/services/chat-block-serializer.ts (toAnthropicContent / fromAnthropicMessage — the pending→plaintext + tool_result→user rules the loop relies on)
    - .planning/phases/04-agentic-loop-citation-ui/04-AI-SPEC.md §3 Entry Point Pattern (lines 194-312 — the verbatim loop body + the critical wire rule)
    - .planning/phases/04-agentic-loop-citation-ui/04-RESEARCH.md Code Examples (lines 284-328), Pitfall 2 (lines 254-258), Pitfall 5 (lines 272-275), Open Question #1 (lines 407-410 — persist auto-executed query tool_use as status 'approved', not 'pending')
    - .planning/phases/04-agentic-loop-citation-ui/04-PATTERNS.md "chat.service.ts (MODIFY)" (lines 131-168)
    - .planning/phases/04-agentic-loop-citation-ui/04-VALIDATION.md §1 (E2 — the agentic loop sampling points)
  </read_first>
  <behavior>
    - RED first. Extend chat.service.spec.ts driving a MOCKED AnthropicApiService.sendMessage returning crafted Message objects (mirror the existing mock pattern). Sample EVERY branch (E2 Nyquist points):
      - stop_reason 'end_turn' → loop completes, emits { kind:'done', stopReason:'end_turn' }
      - stop_reason 'stop_sequence' → completes, done
      - stop_reason 'max_tokens' → completes, done with max_tokens (truncation)
      - stop_reason 'refusal' → completes, done refusal, NO retry
      - stop_reason 'pause_turn' → re-sends the SAME messages unmodified, does NOT net-decrement maxAgentTurns (assert send call count + identical messages on resume)
      - stop_reason 'tool_use' with a query_* block → dispatches via toolRegistry.dispatch, appends tool_result, the tool_result-carrying message role is 'user', re-calls
      - maxAgentTurns reached (mock returns 'tool_use' every turn) → emits { kind:'turn_limit' } and makes ONE final create WITHOUT tools (assert the final send omits `tools`)
      - write proposal mid-loop (mock isWriteProposal true for 'memory') → surfacePendingProposal called, a synthetic 'not yet persisted' tool_result fed, loop does NOT block and finishes
      - teardown: unsubscribe → next mock turn is NOT requested (cancelled flag stops the loop)
  </behavior>
  <action>
    Add `runAgenticLoop(conversationId: string, apiKey: string): Observable<ChatTurnEvent>` to `ChatService`. Copy the canonical body VERBATIM from 04-AI-SPEC §3 Entry Point Pattern (lines 194-309), adapting symbol names to the existing service (`this.anthropicApi`, `this.toolRegistry`, `this.aiSettings`/`AISettingsService`, `this.fitnessContext`, `CLAUDE_MODELS`). Key invariants (do NOT deviate):
    - `new Observable<ChatTurnEvent>(subscriber => { let cancelled = false; (async () => { … })(); return () => { cancelled = true; }; })` — the teardown sets `cancelled`, the `for` loop checks `turn < maxAgentTurns && !cancelled` each iteration (the takeUntilDestroyed cancellation seam, async-first design 04-AI-SPEC §4b).
    - `const maxAgentTurns = settings.maxAgentTurns ?? 10;` `const model = settings.selectedModel ?? CLAUDE_MODELS[0].value;` `const tools = this.toolRegistry.definitions();` `const system = await firstValueFrom(this.fitnessContext.buildSystemPrompt());`
    - Build the working `messages: MessageParam[]` from `this.buildApiMessages(conversation)` (REUSE — do not re-solve tool_result placement).
    - `for (let turn = 0; turn < maxAgentTurns && !cancelled; turn++)`: await `this.anthropicApi.sendMessage(apiKey, { model, max_tokens: settings.maxResponseTokens ?? 4096, system, messages, tools })`. On SDK error → `subscriber.error(err)` (typed AnthropicApiError → RxJS error channel).
    - Emit `{ kind:'assistant_text', blocks: fromAnthropicMessage(response) }`; persist the assistant turn; push `{ role:'assistant', content: response.content }`.
    - `switch (response.stop_reason)`: `end_turn`/`stop_sequence`/`max_tokens`/`refusal` → emit done + complete + return; `pause_turn` → `turn--; continue;` (re-send unmodified — Pitfall 2); `tool_use` → break; `default` → emit done + complete + return (no `default:` should be hit in practice — Pitfall 2 warning sign).
    - Tool execution: filter `tool_use` blocks. For each: emit `{ kind:'tool_use_started', toolUseId: tu.id, toolName: tu.name, input: tu.input }`. If `this.toolRegistry.isWriteProposal(tu.name)` → call `this.surfacePendingProposal(conversationId, tu)` and push a synthetic `{ type:'tool_result', tool_use_id: tu.id, content: 'Proposal surfaced to the user for approval and is NOT yet persisted. Do not assume it was saved; continue your answer using only confirmed data.' }`, then `continue` (D-03 — NEVER block). Else auto-execute: `const result = await this.toolRegistry.dispatch(tu.name, tu.input)`; push `{ type:'tool_result', tool_use_id: tu.id, content: result }`; emit `{ kind:'tool_result', toolUseId: tu.id, summary: result }`. On dispatch throw → push `{ type:'tool_result', tool_use_id: tu.id, content: 'Error: '+msg, is_error: true }` (CHAT-11 recoverable).
    - `messages.push({ role:'user', content: toolResults })` — tool_result ALWAYS a user turn.
    - After the loop (cap reached, !cancelled): emit `{ kind:'turn_limit', turnsUsed: maxAgentTurns }`, then make ONE final `this.anthropicApi.sendMessage(apiKey, { model, max_tokens, system, messages })` WITHOUT `tools` for a best-effort answer (D-04), emit its assistant_text + done, complete.
    - Persistence of auto-executed query tool_use (04-RESEARCH Open Q #1): persist them paired with their real tool_result as `status: 'approved'` (reuse the `approveToolUseBlock` shape / a sibling persist helper), NOT `'pending'` — so the serializer emits a real wire tool_use on replay, not a plain-text placeholder. Add a serializer-replay assertion in the spec if feasible.

    Add the private `surfacePendingProposal(conversationId: string, tu): void` helper — persists the proposed write block as a `status:'pending'` tool_use block on the conversation (reuse the existing block-append primitive the chat-page pending-pill flow already uses) so it renders as a pending pill; it does NOT persist any write.

    Inject `ToolRegistryService` into the constructor (the Phase 3 grep-gate forbidding this import is INTENTIONALLY lifted in Phase 4 — note this in the SUMMARY). Retire `estimateTokens` (26-28) from the window decision: where the window/summarize decision currently uses `estimateTokens`, switch to `this.anthropicApi.countTokens(...)` (call once per request, cache the count — Pitfall 7; do NOT call per keystroke). Leave the existing single-shot `sendMessage` path intact for now (Plan 06 rewires chat-page to the loop).

    GREEN: implement until the spec passes.
  </action>
  <verify>
    <automated>ng test --no-watch --browsers=ChromeHeadless --include='**/chat.service.spec.ts'</automated>
  </verify>
  <acceptance_criteria>
    - chat.service.spec.ts runs green (exit 0)
    - `grep -q "runAgenticLoop" src/app/services/chat.service.ts` succeeds
    - `grep -q "while\|for (let turn" src/app/services/chat.service.ts` shows the bounded loop; the loop condition references `maxAgentTurns` and `cancelled`
    - The spec drives ALL of: end_turn, stop_sequence, max_tokens, refusal, pause_turn, tool_use, the cap, AND a write proposal (grep the spec for each stop_reason string)
    - Spec asserts the tool_result-carrying message role is `'user'` (49e275b regression)
    - Spec asserts the final post-cap send omits `tools` (D-04 best-effort)
    - Spec asserts unsubscribe stops further mock sendMessage calls (cancelled seam)
    - `grep -q "isWriteProposal" src/app/services/chat.service.ts` succeeds (D-03 path)
    - `grep -q "estimateTokens" src/app/services/chat.service.ts` — if still present, the spec/grep proves it is NOT the window-decision driver (countTokens is); prefer removing it entirely
    - `grep -q "@anthropic-ai/sdk" src/app/services/chat.service.ts` — only SDK *type* imports are allowed if any; the loop logic stays SDK-agnostic via the serializer + ToolDefinition (D-17). Confirm no SDK client/runtime import.
    - `ng build --configuration=production` exits 0
  </acceptance_criteria>
  <done>runAgenticLoop handles every StopReason, the cap degrades to a tools-less best-effort answer, write proposals never block, tool_result is always a user turn, and the window uses countTokens not len/4 — all proven by E2 spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model response stop_reason → loop control | Untrusted model-controlled signal drives loop continuation |
| model tool_use blocks → dispatch | Model-chosen tool name + input cross into execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Denial of Service (wedged loop / cost exhaustion) | runAgenticLoop | mitigate | Every `StopReason` branch handled explicitly; `tool_use` is the only continue branch; `maxAgentTurns` cap degrades to a single tools-less best-effort answer (D-04). `takeUntilDestroyed` → `cancelled` stops the loop on navigate-away (stops billing). E2 spec drives every branch + the cap + teardown. |
| T-04-04-02 | Tampering (unfinished tool_use / API 400) | tool_result placement | mitigate | tool_result ALWAYS pushed in a `user` turn (reuses buildApiMessages 49e275b machinery). A mid-loop write proposal is fed a synthetic 'not yet persisted' tool_result so the model never waits forever (D-03 / Pitfall 5). Spec asserts user-turn role. |
| T-04-04-03 | Elevation of Privilege (unapproved write) | isWriteProposal branch | mitigate | A model-proposed write (memory) NEVER auto-executes — it surfaces a pending pill (status:'pending') and is not persisted; only Plan 06's pill-approve flow persists it. query_* are read-only (Plan 02). |
| T-04-04-04 | Tampering (tool dispatch error) | dispatch try/catch | mitigate | A dispatch throw is caught and returned as `is_error:true` tool_result so the model can recover (CHAT-11), never a throw-through that crashes the loop. |
</threat_model>

<verification>
- `ng test --no-watch --browsers=ChromeHeadless` full suite green (no regression to Phase 3 baseline)
- `ng build --configuration=production` exits 0
- D-17: no SDK client/runtime import in chat.service.ts (type-only at most; loop logic SDK-agnostic)
</verification>

<success_criteria>
- runAgenticLoop is a bounded multi-emit Observable handling every StopReason (E2)
- query_* auto-execute; write proposals defer without blocking (D-02/D-03)
- Cap degrades visibly; tool_result always a user turn; window uses countTokens
</success_criteria>

<output>
After completion, create `.planning/phases/04-agentic-loop-citation-ui/04-04-SUMMARY.md`
</output>
